# Dependencies
node_modules/
package-lock.json
yarn.lock

# Build outputs
dist/
build/
.next/
out/

# Environment variables
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temporary files
*.tmp
*.temp
.cache/
~$*

# Backup files
*-backup.html
*.bak

# 用户行为看板运行时文件
用户行为看板（周度）/node_modules/
用户行为看板（周度）/*.db
用户行为看板（周度）/*.sqlite
.vercel
.env*.local

# 用户行为看板大文件（改由 GitHub Release 提供，不提交到仓库）
用户行为看板（周度）/data/*.json
用户行为看板（周度）/*.xlsx
用户行为看板（周度）/*.csv
# 半角括号路径（避免大 CSV/Excel 被提交）
用户行为看板(周度)/*.xlsx
用户行为看板(周度)/*.csv
**/用户行为日志*.csv
**/用户行为日志*.xlsx
