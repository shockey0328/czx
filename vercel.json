{
  "routes": [
    {
      "src": "/搜索数据看板（周度）/(.*)",
      "dest": "/搜索数据看板（周度）/$1"
    },
    {
      "src": "/各模块渗透率看板（月度）/(.*)",
      "dest": "/各模块渗透率看板（月度）/$1"
    },
    {
      "src": "/核心数据看板（周度）/(.*)",
      "dest": "/核心数据看板（周度）/$1"
    },
    {
      "src": "/核心数据看板（月度）/(.*)",
      "dest": "/核心数据看板（月度）/$1"
    }
  ]
}
