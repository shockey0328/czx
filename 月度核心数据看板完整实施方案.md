# 月度核心数据看板完整实施方案

## 概述

本文档提供月度核心数据看板的完整升级方案，包括所有代码和实施步骤。

## 一、数据准备

### 1.1 确保CSV文件包含足够数据
需要至少13个月的数据（用于12个月趋势+1个月同比计算）

### 1.2 B端数据CSV格式
创建 `B端核心数据.csv`：
```csv
年份,月份,签约金额,开票金额,到款金额,订单数,新签,续签
25年,1月,45.6万,38.2万,42.1万,12,5,7
25年,2月,52.3万,41.5万,38.9万,15,6,9
```

### 1.3 运行数据转换
```bash
更新数据.bat
```

## 二、核心功能实现

### 2.1 同比数据计算

在JavaScript中添加以下函数：

```javascript
// 获取去年同月数据
function getYoYData(currentData) {
    const targetYear = currentData.年份 - 1;
    const targetMonth = currentData.月份;
    
    return monthlyData.find(d => 
        d.年份 === targetYear && d.月份 === targetMonth
    );
}

// 计算同比变化
function calculateYoY(current, lastYear) {
    if (!lastYear) return null;
    const change = ((current - lastYear) / lastYear * 100).toFixed(1);
    return (change >= 0 ? '+' : '') + change + '%';
}

// 计算百分点变化（用于百分比指标）
function calculateYoYpp(current, lastYear) {
    if (!lastYear) return null;
    const change = (current - lastYear).toFixed(1);
    return (change >= 0 ? '+' : '') + change + 'pp';
}
```

### 2.2 更新指标卡片显示

修改 `updateMetrics()` 函数：

```javascript
function updateMetrics() {
    const data = monthlyData[currentMonth];
    const prevData = currentMonth > 0 ? monthlyData[currentMonth - 1] : null;
    const yoyData = getYoYData(data);
    
    const metrics = [
        { 
            label: '月活用户', 
            value: data.月活.toLocaleString(), 
            wow: prevData ? calculateChange(data.月活, prevData.月活) : null,
            yoy: yoyData ? calculateYoY(data.月活, yoyData.月活) : null
        },
        { 
            label: '次月留存率', 
            value: data.次月留存 + '%', 
            wow: prevData ? calculateChangepp(data.次月留存, prevData.次月留存) : null,
            yoy: yoyData ? calculateYoYpp(data.次月留存, yoyData.次月留存) : null
        },
        // ... 其他指标
    ];
    
    const grid = document.getElementById('metricsGrid');
    grid.innerHTML = metrics.map(m => `
        <div class="metric-card">
            <div class="label">${m.label}</div>
            <div class="value">${m.value}</div>
            <div class="changes">
                ${m.wow ? `<span class="change-item ${getChangeClass(m.wow)}">环比 ${m.wow}</span>` : ''}
                ${m.yoy ? `<span class="change-item ${getChangeClass(m.yoy)}">同比 ${m.yoy}</span>` : ''}
            </div>
        </div>
    `).join('');
}

// 获取变化样式类（中国风格：红涨绿跌）
function getChangeClass(change) {
    if (change.includes('+')) return 'positive';  // 红色
    if (change.includes('-')) return 'negative';  // 绿色
    return '';
}
```

### 2.3 时间范围切换

添加HTML：

```html
<div class="trends-header">
    <h3 class="chart-title">趋势分析</h3>
    <div class="period-selector">
        <button class="period-btn active" data-months="3">近3个月</button>
        <button class="period-btn" data-months="6">近6个月</button>
        <button class="period-btn" data-months="12">近12个月</button>
    </div>
</div>
```

添加JavaScript：

```javascript
// 设置时间范围选择器
document.querySelectorAll('.period-btn').forEach(btn => {
    btn.addEventListener('click', function() {
        document.querySelectorAll('.period-btn').forEach(b => b.classList.remove('active'));
        this.classList.add('active');
        updateCharts();
    });
});

// 获取过滤后的数据
function getFilteredData() {
    const months = parseInt(document.querySelector('.period-btn.active').dataset.months);
    const currentIndex = monthlyData.findIndex(d => 
        d.年份 === currentMonth.year && d.月份 === currentMonth.month
    );
    
    return monthlyData.slice(
        Math.max(0, currentIndex - months + 1),
        currentIndex + 1
    );
}

// 更新图表（只显示月活、营收、留存率）
function updateCharts() {
    const filteredData = getFilteredData();
    
    updateMAUChart(filteredData);
    updateRevenueChart(filteredData);
    updateRetentionChart(filteredData);
    // 移除ARPPU图表
}
```

### 2.4 B端数据展示

添加HTML：

```html
<div class="chart-section">
    <h3 class="chart-title">B端核心数据</h3>
    <div class="b2b-grid" id="b2bMetrics">
        <!-- 动态生成 -->
    </div>
</div>
```

添加JavaScript：

```javascript
// 更新B端指标
function updateB2BMetrics() {
    if (!bData || bData.length === 0) return;
    
    const currentB2B = bData.find(d => 
        d.年份 === currentMonth.year && d.月份 === currentMonth.month
    );
    
    if (!currentB2B) return;
    
    const metrics = [
        { label: '签约金额', value: currentB2B.签约金额 },
        { label: '开票金额', value: currentB2B.开票金额 },
        { label: '到款金额', value: currentB2B.到款金额 },
        { label: '订单数', value: currentB2B.订单数 },
        { label: '新签', value: currentB2B.新签 },
        { label: '续签', value: currentB2B.续签 }
    ];
    
    const grid = document.getElementById('b2bMetrics');
    grid.innerHTML = metrics.map(m => `
        <div class="b2b-metric-card">
            <div class="b2b-metric-label">${m.label}</div>
            <div class="b2b-metric-value">${m.value}</div>
        </div>
    `).join('');
}
```

### 2.5 AI分析集成

添加HTML：

```html
<div class="chart-section ai-analysis">
    <div class="ai-header">
        <h3 class="chart-title">🤖 AI数据分析</h3>
        <button class="refresh-btn" onclick="generateAIAnalysis()">刷新分析</button>
    </div>
    <div class="ai-content" id="aiAnalysis">
        <div class="loading">正在分析数据...</div>
    </div>
    <div class="ai-footer">
        <span class="ai-credit">由 DeepSeek AI 提供技术支持</span>
    </div>
</div>
```

添加JavaScript：

```javascript
// 生成AI分析
async function generateAIAnalysis() {
    const analysisDiv = document.getElementById('aiAnalysis');
    analysisDiv.innerHTML = '<div class="loading">正在分析数据...</div>';
    
    const currentData = monthlyData[currentMonth];
    const prevData = currentMonth > 0 ? monthlyData[currentMonth - 1] : null;
    
    try {
        const response = await fetch('https://api.deepseek.com/v1/chat/completions', {
            method: 'POST',
            headers: {
                'Authorization': 'Bearer YOUR_API_KEY',
                'Content-Type': 'application/json'
            },
            body: JSON.stringify({
                model: 'deepseek-chat',
                messages: [{
                    role: 'system',
                    content: '你是一个专业的数据分析师，擅长分析月度数据并提供洞察和建议。'
                }, {
                    role: 'user',
                    content: `分析以下月度数据：
                    当前月份：${currentData.年份}年${currentData.月份}
                    月活：${currentData.月活}
                    留存率：${currentData.次月留存}%
                    营收：${currentData.营收}
                    ARPU：${currentData.ARPU}
                    
                    请提供：1.关键变化分析 2.增长趋势分析 3.运营策略建议`
                }],
                temperature: 0.7,
                max_tokens: 1000
            })
        });
        
        const result = await response.json();
        const analysis = result.choices[0].message.content;
        
        displayAIAnalysis(analysis);
    } catch (error) {
        console.error('AI分析失败:', error);
        analysisDiv.innerHTML = '<div class="error">AI分析暂时不可用</div>';
    }
}

// 显示AI分析结果
function displayAIAnalysis(analysis) {
    const analysisDiv = document.getElementById('aiAnalysis');
    
    // 解析分析结果并格式化显示
    analysisDiv.innerHTML = `
        <div class="ai-grid">
            <div class="analysis-section">
                <h5>关键变化</h5>
                <div class="analysis-content">
                    ${formatAnalysisSection(analysis, '关键变化')}
                </div>
            </div>
            <div class="analysis-section">
                <h5>趋势分析</h5>
                <div class="analysis-content">
                    ${formatAnalysisSection(analysis, '趋势')}
                </div>
            </div>
            <div class="analysis-section">
                <h5>策略建议</h5>
                <div class="analysis-content">
                    ${formatAnalysisSection(analysis, '建议')}
                </div>
            </div>
        </div>
    `;
}
```

### 2.6 小橙子AI助手

添加HTML：

```html
<!-- 小橙子AI助手 -->
<div class="ai-assistant">
    <div class="ai-assistant-button" id="aiAssistantBtn">
        <img src="小橙子.png" alt="小橙子" class="ai-assistant-avatar">
        <div class="ai-assistant-pulse"></div>
    </div>
    
    <div class="ai-chat-modal" id="aiChatModal">
        <div class="ai-chat-container">
            <div class="ai-chat-header">
                <div class="ai-chat-avatar">
                    <img src="小橙子.png" alt="小橙子">
                </div>
                <div class="ai-chat-info">
                    <h4>小橙子</h4>
                    <span>AI数据分析助手</span>
                </div>
                <button class="ai-chat-close" id="aiChatClose">×</button>
            </div>
            
            <div class="ai-chat-messages" id="aiChatMessages">
                <!-- 对话消息 -->
            </div>
            
            <div class="ai-chat-input-area">
                <div class="ai-chat-suggestions">
                    <button class="suggestion-btn">分析本月数据</button>
                    <button class="suggestion-btn">营收变化原因</button>
                    <button class="suggestion-btn">提升建议</button>
                </div>
                <div class="ai-chat-input-container">
                    <input type="text" id="aiChatInput" placeholder="请输入您的问题...">
                    <button id="aiChatSend" class="ai-chat-send-btn">发送</button>
                </div>
            </div>
        </div>
    </div>
</div>
```

添加JavaScript：

```javascript
// 初始化AI助手
function setupAIAssistant() {
    const assistantBtn = document.getElementById('aiAssistantBtn');
    const chatModal = document.getElementById('aiChatModal');
    const closeBtn = document.getElementById('aiChatClose');
    const sendBtn = document.getElementById('aiChatSend');
    const input = document.getElementById('aiChatInput');
    
    // 打开/关闭对话框
    assistantBtn.addEventListener('click', () => {
        chatModal.classList.toggle('show');
    });
    
    closeBtn.addEventListener('click', () => {
        chatModal.classList.remove('show');
    });
    
    // 发送消息
    sendBtn.addEventListener('click', () => sendMessage());
    input.addEventListener('keypress', (e) => {
        if (e.key === 'Enter') sendMessage();
    });
    
    // 快捷问题
    document.querySelectorAll('.suggestion-btn').forEach(btn => {
        btn.addEventListener('click', function() {
            input.value = this.textContent;
            sendMessage();
        });
    });
    
    // 初始欢迎消息
    addMessage('ai', '您好！我是小橙子🍊，您的AI数据分析助手。有什么可以帮您的吗？');
}

// 发送消息
async function sendMessage() {
    const input = document.getElementById('aiChatInput');
    const message = input.value.trim();
    
    if (!message) return;
    
    // 显示用户消息
    addMessage('user', message);
    input.value = '';
    
    // 显示输入中状态
    showTyping();
    
    try {
        // 调用AI API
        const response = await fetch('https://api.deepseek.com/v1/chat/completions', {
            method: 'POST',
            headers: {
                'Authorization': 'Bearer YOUR_API_KEY',
                'Content-Type': 'application/json'
            },
            body: JSON.stringify({
                model: 'deepseek-chat',
                messages: [
                    {
                        role: 'system',
                        content: '你是小橙子，一个友好的AI数据分析助手。当前数据：' + JSON.stringify(monthlyData[currentMonth])
                    },
                    {
                        role: 'user',
                        content: message
                    }
                ]
            })
        });
        
        const result = await response.json();
        const reply = result.choices[0].message.content;
        
        hideTyping();
        addMessage('ai', reply);
    } catch (error) {
        hideTyping();
        addMessage('ai', '抱歉，我暂时无法回答。请稍后再试。');
    }
}

// 添加消息到对话框
function addMessage(type, content) {
    const messagesDiv = document.getElementById('aiChatMessages');
    const time = new Date().toLocaleTimeString('zh-CN', { hour: '2-digit', minute: '2-digit' });
    
    const messageHTML = `
        <div class="ai-message ${type}">
            ${type === 'ai' ? `
                <div class="ai-message-avatar">
                    <img src="小橙子.png" alt="小橙子">
                </div>
            ` : `
                <div class="user-message-avatar">我</div>
            `}
            <div class="ai-message-content">
                <div class="ai-message-bubble">${content}</div>
                <div class="ai-message-time">${time}</div>
            </div>
        </div>
    `;
    
    messagesDiv.insertAdjacentHTML('beforeend', messageHTML);
    messagesDiv.scrollTop = messagesDiv.scrollHeight;
}
```

## 三、样式补充

添加以下CSS样式：

```css
/* 变化指标样式（中国风格：红涨绿跌） */
.changes {
    display: flex;
    gap: 8px;
    margin-top: 8px;
    flex-wrap: wrap;
}

.change-item {
    font-size: 11px;
    padding: 2px 6px;
    border-radius: 3px;
    background: #f1f3f4;
}

.change-item.positive {
    background: #ffebee;
    color: #d32f2f;  /* 红色表示上涨 */
}

.change-item.negative {
    background: #e8f5e8;
    color: #388e3c;  /* 绿色表示下跌 */
}

/* 时间范围选择器 */
.trends-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 16px;
}

.period-selector {
    display: flex;
    gap: 8px;
}

.period-btn {
    padding: 6px 16px;
    border: 1px solid #e0e0e0;
    background: white;
    color: #666;
    border-radius: 4px;
    cursor: pointer;
    font-size: 13px;
    transition: all 0.2s;
}

.period-btn:hover,
.period-btn.active {
    background: #ff7043;
    color: white;
    border-color: #ff7043;
}

/* B端数据卡片 */
.b2b-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 12px;
}

.b2b-metric-card {
    background: #f8f9fa;
    border-radius: 8px;
    padding: 16px;
    text-align: center;
    border: 2px solid #FFA366;
}

.b2b-metric-label {
    font-size: 12px;
    color: #666;
    margin-bottom: 8px;
}

.b2b-metric-value {
    font-size: 18px;
    font-weight: 700;
    color: #ff7043;
}

/* AI分析样式 */
.ai-analysis {
    margin-top: 24px;
}

.ai-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 16px;
}

.refresh-btn {
    padding: 6px 16px;
    border: 1px solid #e0e0e0;
    background: white;
    color: #666;
    border-radius: 4px;
    cursor: pointer;
    font-size: 13px;
}

.refresh-btn:hover {
    background: #ff7043;
    color: white;
    border-color: #ff7043;
}

.ai-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
}

.analysis-section h5 {
    color: #ff7043;
    margin-bottom: 12px;
    font-size: 14px;
}

.analysis-content {
    background: #f8f9fa;
    border-radius: 6px;
    padding: 12px;
    font-size: 12px;
    line-height: 1.6;
}

/* 小橙子助手样式 */
.ai-assistant {
    position: fixed;
    left: 30px;
    top: 50%;
    transform: translateY(-50%);
    z-index: 1000;
}

.ai-assistant-button {
    width: 70px;
    height: 70px;
    border-radius: 50%;
    background: white;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 6px 25px rgba(255, 107, 53, 0.25);
    border: 3px solid #ff7043;
    transition: all 0.3s;
}

.ai-assistant-button:hover {
    transform: scale(1.1);
}

.ai-assistant-avatar {
    width: 54px;
    height: 54px;
    object-fit: contain;
}

.ai-assistant-pulse {
    position: absolute;
    width: 100%;
    height: 100%;
    border-radius: 50%;
    background: rgba(255, 107, 53, 0.2);
    animation: pulse 2s infinite;
}

@keyframes pulse {
    0% {
        transform: scale(1);
        opacity: 1;
    }
    100% {
        transform: scale(1.4);
        opacity: 0;
    }
}

/* AI对话框样式 */
.ai-chat-modal {
    position: fixed;
    left: 30px;
    top: 50%;
    transform: translateY(-50%);
    width: 380px;
    height: 600px;
    background: white;
    border-radius: 16px;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
    display: none;
    z-index: 1001;
}

.ai-chat-modal.show {
    display: block;
    animation: slideIn 0.3s ease;
}

@keyframes slideIn {
    from {
        opacity: 0;
        transform: translateY(-50%) translateX(-20px);
    }
    to {
        opacity: 1;
        transform: translateY(-50%) translateX(0);
    }
}

/* 响应式设计 */
@media (max-width: 768px) {
    .ai-grid,
    .b2b-grid {
        grid-template-columns: 1fr;
    }
    
    .ai-assistant {
        left: 15px;
        bottom: 80px;
        top: auto;
        transform: none;
    }
    
    .ai-chat-modal {
        left: 15px;
        right: 15px;
        top: 50px;
        bottom: 50px;
        width: auto;
        height: auto;
        transform: none;
    }
}
```

## 四、完整实施步骤

1. **备份现有文件**
   ```bash
   copy "核心数据看板（月度）\index-static.html" "核心数据看板（月度）\index-static-backup.html"
   ```

2. **准备B端数据CSV**
   - 创建 `核心数据看板（月度）/public/B端核心数据.csv`
   - 按照格式填入数据

3. **更新数据转换脚本**
   - 确保 `convert_csv_to_js_v2.ps1` 包含B端数据转换
   - 运行 `更新数据.bat`

4. **修改HTML文件**
   - 按照上述代码逐步添加功能
   - 测试每个功能模块

5. **配置API密钥**
   - 替换代码中的 `YOUR_API_KEY`
   - 或使用环境变量

6. **测试验证**
   - 测试所有功能
   - 检查响应式布局
   - 验证数据准确性

## 五、注意事项

1. **API密钥安全**：不要将API密钥提交到代码仓库
2. **数据格式**：确保CSV格式正确，特别是百分号和单位
3. **浏览器兼容**：测试主流浏览器
4. **性能优化**：大数据量时考虑分页或懒加载
5. **错误处理**：添加完善的错误提示

## 六、后续优化建议

1. 添加数据导出功能（Excel/PDF）
2. 支持自定义时间范围选择
3. 增加数据对比功能（多月对比）
4. 优化AI分析的准确性
5. 添加数据缓存机制
6. 实现离线模式
7. 添加用户偏好设置

---

**实施完成后，请运行完整测试并更新文档！**
