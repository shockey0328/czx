- 修复橙子学月度核心数据看板更新失败：test-dmp-mcp.xkw.com/maxcompute-mcp 返回 502，改为走 warehouseClient，按 WAREHOUSE_ENGINE 自动选择引擎；当前 env 默认走 Hologres MCP（可用）。
- 调整 `update_core_metrics_from_odps.mjs`：使用 `createWarehouseClient` + `client.runSql`，并修复次月留存 SQL 的整数除法为 `* 1.0 /`，保证 Hologres 下返回正确比例。
- 已执行无 dry-run 更新，成功追加 26年8月 月度核心数据并同步生成 data.js / public/data.js。
- 渗透率（月度）脚本也改走 createWarehouseClient + runSql（Hologres），dry-run 通过；核心月度 .env 显式加 WAREHOUSE_ENGINE=hologres 锁定。
- 分省（月度）因 Hologres 无 dmp_cdm.ip_parse UDF、xyiolog 也无 province 字段，无法等价平移。与用户约定新流程：用户本地 MaxCompute 直查 + 我更新看板。已生成 `分省数据看板（月度）/sql/province_2026-08.sql`（4 条 SQL，ip_parse 解析省份，嵌入 52 个排除 payer_id），待用户返回结果后更新 趋势分析/26年8月.xlsx 与 trend-data.js。趋势分析 Excel 实际已有 26年1~7月（6、7月都在，之前 head 截断误判为仅到 5 月），缺口仅 8 月；核心/渗透率看板已到 8 月。
- 用户要求把分省 4 条 SQL 合并为单条。已生成 `分省数据看板（月度）/sql/province_2026-08_merged.sql`：用 CTE 分别算 active/newuser/revenue/used 四个指标（均按 ip_parse 解析省份），再用 all_p 收集所有出现省份并 LEFT JOIN 拼成「一行一省、列=province,pv,uv,new_user_uv,paid_uv,total_amount,user_count」。修了一个生成 bug：脚本读 excluded_payer_ids.txt 未过滤 `#` 注释行，导致 3 行注释污染进 NOT IN 列表，已删除。原 4 条 province_2026-08.sql 保留作对照。
- 用户返回 26年8月 分省 7 列宽表。已生成 `趋势分析/26年8月.xlsx`（34 省标准清单，列=名称/活跃用户/新用户/营收/使用用户/ARPU/使用率），并执行 `node 趋势分析/build_trend_data.js` 重建 `trend-data.js`；同比/环比已自动计算，月份序列扩展到 20 个月（最新 26年8月）。清洗时丢弃了空省份行与海外地区，与 26年7月口径保持一致。
