# 橙子学数据看板 — 交互说明

本文描述当前仓库内**已实现**的交互行为，便于产品、设计与开发对齐；与门户 `index.html` + `main.js` 及各子看板源码一致。

---

## 一、统一门户（根目录 `index.html`）

| 控件 | 交互 | 结果 |
| --- | --- | --- |
| **周度 / 月度** 切换按钮 | 单击 | 切换 `currentPeriod`；更新顶部 **看板类型** 下拉选项为对应周期列表；自动加载该周期**列表第一项**看板。 |
| **看板类型** 下拉框 | 变更选项 | 在**当前周期**下，用 `iframe` 加载所选子看板 HTML（路径见 `main.js` 中 `DASHBOARDS`）。 |
| **主内容区** | — | 先显示「正在加载看板…」，约 200ms 后创建 `iframe`；`src` 带 `v=PORTAL_BUILD` 参数，减轻子页缓存导致样式/脚本不更新问题。 |
| **加载失败** | `iframe` 异常等 | 展示错误区与「尝试直接打开」链接（指向解析后的 URL）。 |

**访问方式**：须通过 HTTP(S) 打开门户（见 `README.md`）；`file://` 下 `iframe` 与部分子看板可能受限。

---

## 二、周度子看板（门户「周度」下四项）

### 2.1 周度核心数据（`核心数据看板（周度）/index.html`）

| 控件 | 交互 | 结果 |
| --- | --- | --- |
| **选择周度** 下拉 | 变更 | 切换当前统计周；刷新四宫格指标、趋势、热力等与周相关的模块。 |
| **复制核心指标** 按钮 | 单击 | 将本周核心指标文案复制到剪贴板（需浏览器权限）。 |
| **趋势区间** 按钮（近 2/3/4 周） | 单击 | 切换趋势图所覆盖的周数；当前选中项有高亮样式。 |
| **智能分析 / 刷新分析** | 单击 | 切换或刷新 AI 分析区域展示逻辑（与页面内分析容器联动）。 |
| **小橙子 AI 悬浮按钮** | 单击 | 打开/关闭 AI 对话浮层。 |
| **建议问题** 按钮 | 单击 | 将预设问题填入对话或触发发送（与实现一致）。 |
| **对话输入框 + 发送** | 发送 | 提交对话（依赖页面内 AI 接入配置）。 |
| **图表**（Chart.js / ECharts） | 悬停等 | 以各图配置为准：通常含 tooltip、图例点击显隐等。 |

### 2.2 用户增长（`用户增长数据看板（周度）/user-growth-dashboard.html`）

| 控件 | 交互 | 结果 |
| --- | --- | --- |
| **选择周度** 下拉 | 变更 | 切换 `selectedWeekIdx`；刷新 KPI、日趋势、渠道柱状图、表格与叙事文案等。 |
| **趋势周期** 按钮（当周 / 4 周 / 12 周） | 单击 | 切换日趋势图聚合的时间跨度；更新按钮激活态。 |
| **渠道横向柱图** | 单击某一渠道 | 高亮下方渠道表中对应行（`data-channel` 联动）。 |
| **渠道表行** | 与上图联动 | 被点击渠道行高亮。 |
| **快捷问题** 按钮 | 单击 | 将 `data-q` 中的问题写入对话区或触发分析逻辑。 |
| **对话输入 + 发送** | 单击发送或快捷键 | 提交助手对话（与页面脚本一致）。 |
| **窗口 resize** | 调整窗口 | 防抖后重算图表尺寸，避免挤压变形。 |

### 2.3 搜索数据（`搜索数据看板（周度）/index.html`）

| 控件 | 交互 | 结果 |
| --- | --- | --- |
| **周度选择** 下拉 | 变更 | 切换当前分析周，刷新各模块数据。 |
| **排序 Tab**（按 UV / 按 PV） | 单击 | 切换词云或列表排序维度（与 `data-sort` 一致）。 |
| **时间范围 Tab**（近 7 天～近 5 周等） | 单击 | 切换漏斗/趋势等图的时间窗口。 |
| **转化指标** 下拉 | 变更 | 切换转化率相关展示口径。 |
| **词云/趋势周数 Tab**（近 5/10/15 周、全部） | 单击 | 调整词云或关联图的时间覆盖范围。 |

### 2.4 用户行为（`user-behavior.html` → 实际入口以 `main.js` 为准）

| 说明 |
| --- |
| 依赖 **Node 服务** 与数据库（见 `README.md`）；门户内嵌时须保证同源或服务可达。 |
| 典型交互包括：**时间段筛选**、**用户 ID 查询**、**对话式 AI 分析**（与 `dashboard-db.html` / 服务端实现一致）。具体按钮与字段以该子项目页面为准。 |

---

## 三、月度子看板（门户「月度」下三项）

### 3.1 月度核心（`核心数据看板（月度）/index-static.html`）

| 控件 | 交互 | 结果 |
| --- | --- | --- |
| **时间范围**（3 / 6 / 12 个月） | 单击 | 过滤趋势与卡片数据跨度。 |
| **指标卡片 / 趋势图** | 悬停 | Tooltip 展示数值；具体以 Recharts 配置为准。 |
| **AI 分析 / 小橙子助手** | 依页面 | 与周度类似：打开面板、预设问题、发送消息等（以该静态页内嵌脚本为准）。 |

### 3.2 各模块渗透率（`各模块渗透率看板（月度）/index.html`）

| 控件 | 交互 | 结果 |
| --- | --- | --- |
| **月份** 下拉 | 变更 | 筛选展示月份。 |
| **时间跨度** 下拉（如近 1 年） | 变更 | 调整趋势图时间范围。 |
| **一级模块** 下拉 | 变更 | 筛选模块维度；「全部」展示汇总逻辑。 |

### 3.3 分省数据（`分省数据看板（月度）/index.html`）

| 控件 | 交互 | 结果 |
| --- | --- | --- |
| **省份选择** | 变更 | 切换分省序列与对比（以 `app.js` 为准）。 |
| **时间周期 / 指标维度** | 变更 | 更新排名表与趋势图（见 `README` 中「省份趋势」「排名对比」描述）。 |
| **AI 报告** | 依页面 | 触发生成或展示分析报告（若已接入）。 |

---

## 四、跨看板通用行为

- **键盘**：部分页面支持输入框 **Enter** 发送（如用户增长助手）。
- **响应式**：窄屏下布局由各自 CSS（含门户 `styles.css`、周度主题 `weekly-dashboard-theme.css`）控制，导航可能折叠或换行。
- **数据加载**：多数周度页先 `fetch` CSV，失败时回退内嵌 Base64（如用户增长 `embedded-csv-b64.js`）；异常时以控制台或页面提示为准，**不应静默伪造数据**（与 `.cursorrules` 中数据规则一致）。

---

## 五、维护时注意

- 在门户 **新增子看板**：须改 `main.js` 的 `DASHBOARDS` 与 `index.html` 内初始 `<option>`（若需），并同步本文与 `README.md`。
- **iframe 嵌套限制**：子页勿假设顶层 `window` 为自身；与父页通信若需 `postMessage`，须单独设计（当前以独立运行为主）。

---

*文档版本：与仓库当前结构同步；若某子页改版，请以对应 `index.html` / `*.html` 源码为准并更新本节。*
