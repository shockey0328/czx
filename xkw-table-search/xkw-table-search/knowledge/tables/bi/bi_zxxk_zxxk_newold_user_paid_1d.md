# `bi_zxxk_zxxk_newold_user_paid_1d`

- 层级：`bi`
- 本地表描述：日期
- 主题标签：user
- 数据粒度：按 coalesce(p.application_id, '') 聚合
- 分区字段：无
- 推荐项目：`dmp_ads`（bi/ads）或 `dmp_cdm`（dwd/dim）

## 关键字段

`dt`、`time_grain`、`newuser_paid_amount`

## 字段

| 字段 | 类型 | 说明 | 来源 |
|---|---|---|---|
| `dt` | `STRING` | 日期 | ddl |
| `application_name` | `STRING` | 终端：PC站,M站,全部 | ddl |
| `time_grain` | `STRING` | 统计粒度: 日/周/月 | ddl |
| `newuser_paid_amount` | `decimal(20,2` | 未提供字段注释 | ddl |

## ETL 与查询提示

- 写入方式：overwrite
- 上游表：`dwd.dwd_ump_pay_trd_trade_wide_di`, `dim.dim_pub_pub_user`, `pre1day_tbl`, `pre1week_tbl`, `pre1mth_tbl`, `bi.bi_zxxk_zxxk_newold_user_paid_1d`, `pre1day_lastresult`
- 关联条件：p.user_id = cast(u.user_id as string)
- 过滤条件：p.paid_status = 1 and (to_date(p.dt) = '${dt}' or to_date(p.dt) = add_months('${dt}', -12) or to_date(p.dt) = add_months('${dt}', -24) ) and app_id in ('app_wxzxxk','app_xyzxxk','app_zxxkcashier','app_zxxkplus','app_xuebei') and p.status = 1；p.paid_status = 1 and (to_date(p.dt) = '${dt}' or to_date(p.dt) = add_months('${dt}', -12) or to_date(p.dt) = add_months('${dt}', -24) ) and app_id in ('app_wxzxxk','app_xyzxxk','app_zxxkcashier','app_zxxkplus','app_xuebei') and p.status = 1 ), pre1week_tbl as ( -----time_grain='周'维度下 这周一到dt的数据 select max(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10)) as dt, coalesce(p.application_id, '') as application_id, max('周') as time_grain, sum(if(to_date(p.dt) >= substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10) and to_date(p.dt) <= '${dt}' and to_date(u.user_reg_time) >= p.dt, p.amount * 0.01, 0)) as newuser_paid_amount, sum(if(to_date(p.dt) >= substring(cast(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -12) as string), 1, 10) and to_date(p.dt) < substring(cast(date_add(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -12), 7) as string), 1, 10) and to_date(u.user_reg_time) >= p.dt , p.amount * 0.01, 0)) as newuser_paid_amount_last_1year, sum(if(to_date(p.dt) >= substring(cast(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -24) as string), 1, 10) and to_date(p.dt) < substring(cast(date_add(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -24), 7) as string), 1, 10) and to_date(u.user_reg_time) >= p.dt, p.amount * 0.01, 0)) as newuser_paid_amount_last_2year, sum(if(to_date(p.dt) >= substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10) and to_date(p.dt) <= '${dt}' and to_date(u.user_reg_time) < p.dt, p.amount * 0.01, 0)) as olduser_paid_amount, sum(if(to_date(p.dt) >= substring(cast(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -12) as string), 1, 10) and to_date(p.dt) < substring(cast(date_add(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -12), 7) as string), 1, 10) and to_date(u.user_reg_time) < p.dt , p.amount * 0.01, 0)) as olduser_paid_amount_last_1year, sum(if(to_date(p.dt) >= substring(cast(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -24) as string), 1, 10) and to_date(p.dt) < substring(cast(date_add(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -24), 7) as string), 1, 10) and to_date(u.user_reg_time) < p.dt, p.amount * 0.01, 0)) as olduser_paid_amount_last_2year, count(distinct if(to_date(u.user_reg_time) >= substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), p.user_id, null)) as new_user_cnt, ------新用户数 count(distinct if(to_date(u.user_reg_time) < substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), p.user_id, null)) as old_user_cnt ------老用户数 from ${dwd}.dwd_ump_pay_trd_trade_wide_di p inner join ${dim}.dim_pub_pub_user u on p.user_id = cast(u.user_id as string) where p.paid_status = 1 and ( (to_date(dt) >= substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10) and to_date(dt) <= '${dt}' ) or (to_date(dt) >= substring(cast(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -12) as string), 1, 10) and to_date(dt) < substring(cast(date_add(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -12), 7) as string), 1, 10) ) or (to_date(dt) >= substring(cast(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -24) as string), 1, 10) and to_date(dt) < substring(cast(date_add(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -24), 7) as string), 1, 10) ) ) and app_id in ('app_wxzxxk','app_xyzxxk','app_zxxkcashier','app_zxxkplus','app_xuebei') and p.status = 1；p.paid_status = 1 and ( (to_date(dt) >= substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10) and to_date(dt) <= '${dt}' ) or (to_date(dt) >= substring(cast(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -12) as string), 1, 10) and to_date(dt) < substring(cast(date_add(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -12), 7) as string), 1, 10) ) or (to_date(dt) >= substring(cast(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -24) as string), 1, 10) and to_date(dt) < substring(cast(date_add(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -24), 7) as string), 1, 10) ) ) and app_id in ('app_wxzxxk','app_xyzxxk','app_zxxkcashier','app_zxxkplus','app_xuebei') and p.status = 1 ), pre1mth_tbl as ( -----time_grain='月'维度下，这个月初到dt的数据 select max(concat(substring('${dt}', 1, 7), '-01')) as dt, coalesce(p.application_id, '') as application_id, max('月') as time_grain, sum(if(substring(cast(to_date(p.dt) as string), 1, 7) = substring('${dt}', 1, 7) and substring(cast(to_date(u.user_reg_time) as string), 1, 10) >= p.dt , p.amount * 0.01, 0)) as newuser_paid_amount, sum(if(substring(cast(add_months(cast(to_date(p.dt) as string), 12) as string), 1, 7) = substring('${dt}', 1, 7) and substring(cast(to_date(u.user_reg_time) as string), 1, 10) >= p.dt , p.amount * 0.01, 0)) as newuser_paid_amount_last_1year, sum(if(substring(cast(add_months(cast(to_date(p.dt) as string), 24) as string), 1, 7) = substring('${dt}', 1, 7) and substring(cast(to_date(u.user_reg_time) as string), 1, 10) >= p.dt , p.amount * 0.01, 0)) as newuser_paid_amount_last_2year, sum(if(substring(cast(to_date(p.dt) as string), 1, 7) = substring('${dt}', 1, 7) and substring(cast(to_date(u.user_reg_time) as string), 1, 10) < p.dt , p.amount * 0.01, 0)) as olduser_paid_amount, sum(if(substring(cast(add_months(cast(to_date(p.dt) as string), 12) as string), 1, 7) = substring('${dt}', 1, 7) and substring(cast(to_date(u.user_reg_time) as string), 1, 10) < p.dt , p.amount * 0.01, 0)) as olduser_paid_amount_last_1year, sum(if(substring(cast(add_months(cast(to_date(p.dt) as string), 24) as string), 1, 7) = substring('${dt}', 1, 7) and substring(cast(to_date(u.user_reg_time) as string), 1, 10) < p.dt , p.amount * 0.01, 0)) as olduser_paid_amount_last_2year, count(distinct if(substring(cast(to_date(u.user_reg_time) as string), 1, 7) = substring('${dt}', 1, 7), p.user_id, null)) as new_user_cnt, ------这个月的新用户数 count(distinct if(substring(cast(to_date(u.user_reg_time) as string), 1, 7) < substring('${dt}', 1, 7), p.user_id, null)) as old_user_cnt ------这个月的老用户数 from ${dwd}.dwd_ump_pay_trd_trade_wide_di p inner join ${dim}.dim_pub_pub_user u on p.user_id = cast(u.user_id as string) where p.paid_status = 1 and (substring(cast(to_date(dt) as string), 1, 7) = substring('${dt}', 1, 7) or substring(cast(add_months(cast(to_date(dt) as string), 12) as string), 1, 7) = substring('${dt}', 1, 7) or substring(cast(add_months(cast(to_date(dt) as string), 24) as string), 1, 7) = substring('${dt}', 1, 7) ) and app_id in ('app_wxzxxk','app_xyzxxk','app_zxxkcashier','app_zxxkplus','app_xuebei') and p.status = 1；p.paid_status = 1 and (substring(cast(to_date(dt) as string), 1, 7) = substring('${dt}', 1, 7) or substring(cast(add_months(cast(to_date(dt) as string), 12) as string), 1, 7) = substring('${dt}', 1, 7) or substring(cast(add_months(cast(to_date(dt) as string), 24) as string), 1, 7) = substring('${dt}', 1, 7) ) and app_id in ('app_wxzxxk','app_xyzxxk','app_zxxkcashier','app_zxxkplus','app_xuebei') and p.status = 1 ), pre1day_lastresult as ( select dt, case when coalesce(application_id, '') = 'pczhan' then 'PC站' when coalesce(application_id, '') = 'mzhan' then 'M站' when coalesce(application_id, '') = 'androidapp' then '安卓app' when coalesce(application_id, '') = 'iosapp' then 'iOSapp' when coalesce(application_id, '') = 'weixinxiaochengxu' then '微信小程序' when coalesce(application_id, '') = 'app' then 'App' when coalesce(application_id, '') = 'other' then '其他' when coalesce(application_id, '') = 'quanbu' then '全部' else '' end as application_name, time_grain, cast(newuser_paid_amount as decimal(20,2)) as newuser_paid_amount, cast(newuser_paid_amount_last_1year as decimal(20,2)) as newuser_paid_amount_last_1year, cast(newuser_paid_amount_last_2year as decimal(20,2)) as newuser_paid_amount_last_2year, cast(olduser_paid_amount as decimal(20,2)) as olduser_paid_amount, cast(olduser_paid_amount_last_1year as decimal(20,2)) as olduser_paid_amount_last_1year, cast(olduser_paid_amount_last_2year as decimal(20,2)) as olduser_paid_amount_last_2year, cast(new_user_cnt as int) as new_user_cnt, cast(old_user_cnt as int) as old_user_cnt from ( select * from pre1day_tbl；dt < '${dt}' and time_grain = '天'；substring(dt, 1, 10) < concat(substring('${dt}', 1, 7), '-01') and time_grain = '月'；substring(dt, 1, 10) < substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10) and time_grain = '周'
- 聚合函数：MAX('${dt}'), MAX('天'), SUM(if(to_date(p.dt), COUNT(distinct if(to_date(u.user_reg_time), MAX(substring(cast(date_sub(substring('${dt}', 1, 10), MAX('周'), MAX(concat(substring('${dt}', 1, 7), MAX('月'), SUM(if(substring(cast(to_date(p.dt), SUM(if(substring(cast(add_months(cast(to_date(p.dt), COUNT(distinct if(substring(cast(to_date(u.user_reg_time)

## 使用边界

- 本文件是基于本地 DDL/DML 的辅助元数据，不替代 MaxCompute 实时 schema。
- 执行 SQL 前使用 `mcp__maxcompute__get_table_schema` 核对字段、类型和分区。
- 查询分区表必须在 WHERE 中添加分区过滤，避免全表扫描。

## 数据质量提示

- 字段 newuser_paid_amount 缺少注释
- 未匹配到 INSERT SQL，来源与指标逻辑待补充
