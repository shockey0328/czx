# `bi_pub_pub_res_profile_publish_stats`

- 层级：`bi`
- 本地表描述：计算日期
- 主题标签：content_resource, log_behavior
- 数据粒度：按 substring(publish_time,1,10),stage_id,subject_id,course_id,commercial_level_extend_name ), t2_month as ( select concat(substring(publish_time,1,7),'-01') create_date ,'月' as time_grain ,stage_id ,subject_id ,course_id ,a.commercial_level_extend_name ,count(a.res_id) res_cnt ,0 as res_cnt_last_year ,0 as res_cnt_2_years_ago ,0 as res_cnt_last_period ,count(DISTINCT if(dl_cnt_publish30d>0, res_id, NULL)) AS res_cnt_dl_publish30d ,0 as res_cnt_dl_publish30d_last_year ,0 as res_cnt_dl_publish30d_2_years_ago ,0 as res_cnt_dl_publish30d_last_period ,coalesce(sum(paid_sum_publish30d),0) paid_sum_publish30d ,0 as paid_sum_publish30d_last_year ,0 as paid_sum_publish30d_2_years_ago ,0 as paid_sum_publish30d_last_period from ${bi}.bi_pub_pub_hbd_res_profile_td a where (substring(a.publish_time,1,7) = substring('${dt}',1,7) or substring(a.publish_time,1,7) = substring(add_months('${dt}', -12),1,7) or substring(a.publish_time,1,7) = substring(add_months('${dt}', -24),1,7) or substring(a.publish_time,1,7) = substring(add_months('${dt}', -1),1,7)) and status='P4_2' and source_application_id in ('ewt.pro','ewt.speed','qbm','rbm', 'xiaoxue.zxxk','zxxk','ewangtong','jx.ekt', 'zy.usercenter','zxxk.paper','zy.yfyb','oms','zy.ccw','zy.shop') group by substring(publish_time,1,7),stage_id,subject_id,course_id,commercial_level_extend_name ), t3_week as ( select case when substring(a.publish_time,1,10) >= date_add(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-7) and substring(a.publish_time,1,10) < substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10) then date_add(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-7) when substring(a.publish_time,1,10) >= substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10) then substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10) when (substring(a.publish_time,1,10) >= add_months(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-12) and substring(a.publish_time,1,10) < date_add(add_months(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-12),7) ) then add_months(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-12) when (substring(a.publish_time,1,10) >= add_months(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-24) and substring(a.publish_time,1,10) < date_add(add_months(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-24),7)) then add_months(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-24) end as create_date ,'周' as time_grain ,stage_id ,subject_id ,course_id ,a.commercial_level_extend_name ,count(a.res_id) res_cnt ,0 as res_cnt_last_year ,0 as res_cnt_2_years_ago ,0 as res_cnt_last_period ,count(DISTINCT if(dl_cnt_publish30d>0, res_id, NULL)) AS res_cnt_dl_publish30d ,0 as res_cnt_dl_publish30d_last_year ,0 as res_cnt_dl_publish30d_2_years_ago ,0 as res_cnt_dl_publish30d_last_period ,coalesce(sum(paid_sum_publish30d),0) paid_sum_publish30d ,0 as paid_sum_publish30d_last_year ,0 as paid_sum_publish30d_2_years_ago ,0 as paid_sum_publish30d_last_period from ${bi}.bi_pub_pub_hbd_res_profile_td a where ( substring(a.publish_time,1,10) >= date_add(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-7) or (substring(a.publish_time,1,10) >= add_months(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-12) and substring(a.publish_time,1,10) < date_add(add_months(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-12),7) ) or (substring(a.publish_time,1,10) >= add_months(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-24) and substring(a.publish_time,1,10) < date_add(add_months(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-24),7)) ) and status='P4_2' and source_application_id in ('ewt.pro','ewt.speed','qbm','rbm', 'xiaoxue.zxxk','zxxk','ewangtong','jx.ekt', 'zy.usercenter','zxxk.paper','zy.yfyb','oms','zy.ccw','zy.shop') group by case when substring(a.publish_time,1,10) >= date_add(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-7) and substring(a.publish_time,1,10) < substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10) then date_add(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-7) when substring(a.publish_time,1,10) >= substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10) then substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10) when (substring(a.publish_time,1,10) >= add_months(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-12) and substring(a.publish_time,1,10) < date_add(add_months(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-12),7) ) then add_months(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-12) when (substring(a.publish_time,1,10) >= add_months(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-24) and substring(a.publish_time,1,10) < date_add(add_months(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-24),7)) then add_months(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-24) end,stage_id,subject_id,course_id,commercial_level_extend_name ), t4_result_1 as ( select * from t1_day 聚合
- 分区字段：无
- 推荐项目：`dmp_ads`（bi/ads）或 `dmp_cdm`（dwd/dim）

## 关键字段

`dt`、`time_grain`、`res_cnt`、`res_cnt_last_year`、`res_cnt_2_years_ago`、`res_cnt_last_period`、`res_cnt_dl_publish30d`、`res_cnt_dl_publish30d_last_year`、`res_cnt_dl_publish30d_2_years_ago`、`res_cnt_dl_publish30d_last_period`、`paid_sum_publish30d`

## 字段

| 字段 | 类型 | 说明 | 来源 |
|---|---|---|---|
| `dt` | `STRING` | 计算日期 | ddl |
| `time_grain` | `STRING` | 统计粒度 | ddl |
| `stage_name` | `STRING` | 学段 | ddl |
| `subject_name` | `STRING` | 学科 | ddl |
| `course_name` | `STRING` | 课程名称 | ddl |
| `commercial_level_extend_name` | `STRING` | 商业等级 | ddl |
| `res_cnt` | `int` | 发布资料量 | ddl |
| `res_cnt_last_year` | `int` | 去年同期发布资料量 | ddl |
| `res_cnt_2_years_ago` | `int` | 2年前同期发布资料量 | ddl |
| `res_cnt_last_period` | `int` | 上个周期发布资料量 | ddl |
| `res_cnt_dl_publish30d` | `int` | 发布30天有下载资料量 | ddl |
| `res_cnt_dl_publish30d_last_year` | `int` | 去年同期发布30天有下载资料量 | ddl |
| `res_cnt_dl_publish30d_2_years_ago` | `int` | 2年前同期发布30天有下载资料量 | ddl |
| `res_cnt_dl_publish30d_last_period` | `int` | 上个月同期发布30天有下载资料量 | ddl |
| `paid_sum_publish30d` | `decimal(10,2` | 未提供字段注释 | ddl |

## ETL 与查询提示

- 写入方式：overwrite
- 上游表：`bi.bi_pub_pub_hbd_res_profile_td`, `t1_day`, `t2_month`, `t3_week`, `t4_result_1`, `dim.dim_pub_pub_course`, `dim.dim_pub_pub_stage`, `dim.dim_pub_pub_subject`, `t5_result_2`, `bi.bi_pub_pub_res_profile_publish_stats`
- 关联条件：coalesce(t1.course_id,t2.course_id,t3.course_id,t4.course_id,t5.course_id,t6.course_id) = b1.course_id；coalesce(t1.stage_id,t2.stage_id,t3.stage_id,t4.stage_id,t5.stage_id,t6.stage_id) = b2.stage_id；coalesce(t1.subject_id,t2.subject_id,t3.subject_id,t4.subject_id,t5.subject_id,t6.subject_id) = b3.subject_id
)
insert overwrite table ${bi}.bi_pub_pub_res_profile_publish_stats
select * from t5_result_2
- 过滤条件：(substring(a.publish_time,1,10) = '${dt}' or substring(a.publish_time,1,10) = add_months('${dt}', -12) or substring(a.publish_time,1,10) = add_months('${dt}', -24) or substring(a.publish_time,1,10) = date_add('${dt}', -1)) and status='P4_2' and (source_application_id in ('ewt.pro','ewt.speed','qbm','rbm', 'xiaoxue.zxxk','zxxk','ewangtong','jx.ekt', 'zy.usercenter','zxxk.paper','zy.yfyb','oms','zy.ccw','zy.shop'))；(substring(a.publish_time,1,7) = substring('${dt}',1,7) or substring(a.publish_time,1,7) = substring(add_months('${dt}', -12),1,7) or substring(a.publish_time,1,7) = substring(add_months('${dt}', -24),1,7) or substring(a.publish_time,1,7) = substring(add_months('${dt}', -1),1,7)) and status='P4_2' and source_application_id in ('ewt.pro','ewt.speed','qbm','rbm', 'xiaoxue.zxxk','zxxk','ewangtong','jx.ekt', 'zy.usercenter','zxxk.paper','zy.yfyb','oms','zy.ccw','zy.shop')；( substring(a.publish_time,1,10) >= date_add(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-7) or (substring(a.publish_time,1,10) >= add_months(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-12) and substring(a.publish_time,1,10) < date_add(add_months(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-12),7) ) or (substring(a.publish_time,1,10) >= add_months(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-24) and substring(a.publish_time,1,10) < date_add(add_months(substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10),-24),7)) ) and status='P4_2' and source_application_id in ('ewt.pro','ewt.speed','qbm','rbm', 'xiaoxue.zxxk','zxxk','ewangtong','jx.ekt', 'zy.usercenter','zxxk.paper','zy.yfyb','oms','zy.ccw','zy.shop')；create_date <= add_months('${dt}', -12)) t2 on t1.create_date = add_months(t2.create_date, 12) and t1.time_grain = t2.time_grain and t1.stage_id = t2.stage_id and t1.subject_id = t2.subject_id and t1.course_id = t2.course_id and t1.commercial_level_extend_name = t2.commercial_level_extend_name full join (select * from t4_result_1 where create_date <= add_months('${dt}', -24)) t3 on coalesce(t1.create_date,add_months(t2.create_date, 12) ) = add_months(t3.create_date, 24) and coalesce(t1.time_grain,t2.time_grain) = t3.time_grain and coalesce(t1.stage_id,t2.stage_id) = t3.stage_id and coalesce(t1.subject_id,t2.subject_id) = t3.subject_id and coalesce(t1.course_id,t2.course_id) = t3.course_id and coalesce(t1.commercial_level_extend_name,t2.commercial_level_extend_name) = t3.commercial_level_extend_name full join (select * from t4_result_1 where create_date < date_add('${dt}', -1) and time_grain = '日') t4 on coalesce(t1.create_date,add_months(t2.create_date, 12) ,add_months(t3.create_date, 24) ) = date_add(t4.create_date, 1) and coalesce(t1.time_grain,t2.time_grain,t3.time_grain) = t4.time_grain and coalesce(t1.stage_id,t2.stage_id,t3.stage_id) = t4.stage_id and coalesce(t1.subject_id,t2.subject_id,t3.subject_id) = t4.subject_id and coalesce(t1.course_id,t2.course_id,t3.course_id) = t4.course_id and coalesce(t1.commercial_level_extend_name,t2.commercial_level_extend_name,t3.commercial_level_extend_name) = t4.commercial_level_extend_name full join (select * from t4_result_1 where create_date < add_months('${dt}', -1) and time_grain = '月') t5 on coalesce(t1.create_date,add_months(t2.create_date, 12) ,add_months(t3.create_date, 24) ) = add_months(t5.create_date, 1) and coalesce(t1.time_grain,t2.time_grain,t3.time_grain) = t5.time_grain and coalesce(t1.stage_id,t2.stage_id,t3.stage_id) = t5.stage_id and coalesce(t1.subject_id,t2.subject_id,t3.subject_id) = t5.subject_id and coalesce(t1.course_id,t2.course_id,t3.course_id) = t5.course_id and coalesce(t1.commercial_level_extend_name,t2.commercial_level_extend_name,t3.commercial_level_extend_name) = t5.commercial_level_extend_name full join (select * from t4_result_1 where create_date < substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10) and time_grain = '周') t6 on coalesce(t1.create_date,add_months(t2.create_date, 12) ,add_months(t3.create_date, 24) ) = date_add(t6.create_date, 7) and coalesce(t1.time_grain,t2.time_grain,t3.time_grain) = t6.time_grain and coalesce(t1.stage_id,t2.stage_id,t3.stage_id) = t6.stage_id and coalesce(t1.subject_id,t2.subject_id,t3.subject_id) = t6.subject_id and coalesce(t1.course_id,t2.course_id,t3.course_id) = t6.course_id and coalesce(t1.commercial_level_extend_name,t2.commercial_level_extend_name,t3.commercial_level_extend_name) = t6.commercial_level_extend_name join ${dim}.dim_pub_pub_course b1 on coalesce(t1.course_id,t2.course_id,t3.course_id,t4.course_id,t5.course_id,t6.course_id) = b1.course_id join ${dim}.dim_pub_pub_stage b2 on coalesce(t1.stage_id,t2.stage_id,t3.stage_id,t4.stage_id,t5.stage_id,t6.stage_id) = b2.stage_id join ${dim}.dim_pub_pub_subject b3 on coalesce(t1.subject_id,t2.subject_id,t3.subject_id,t4.subject_id,t5.subject_id,t6.subject_id) = b3.subject_id )；(time_grain = '月' and create_date = concat(substring('${dt}',1,7),'-01')) or (time_grain = '日' and create_date = '${dt}') or (time_grain = '周' and create_date = substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10))；(time_grain = '月' and dt != concat(substring('${dt}',1,7),'-01')) or (time_grain = '周' and dt != substring(cast(date_sub(substring('${dt}',1,10), pmod(datediff(substring('${dt}',1,10), '1900-01-01'), 7)) as string),1,10)) or ( time_grain = '日' and dt != '${dt}' ) ;
- 聚合函数：COUNT(a.res_id), COUNT(DISTINCT if(dl_cnt_publish30d>0, res_id, NULL), SUM(paid_sum_publish30d)

## 使用边界

- 本文件是基于本地 DDL/DML 的辅助元数据，不替代 MaxCompute 实时 schema。
- 执行 SQL 前使用 `mcp__maxcompute__get_table_schema` 核对字段、类型和分区。
- 查询分区表必须在 WHERE 中添加分区过滤，避免全表扫描。

## 数据质量提示

- 字段 paid_sum_publish30d 缺少注释
- 未匹配到 INSERT SQL，来源与指标逻辑待补充
