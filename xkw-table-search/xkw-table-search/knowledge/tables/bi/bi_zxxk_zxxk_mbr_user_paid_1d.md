# `bi_zxxk_zxxk_mbr_user_paid_1d`

- 层级：`bi`
- 本地表描述：日期：天,周,月
- 主题标签：user
- 数据粒度：按 coalesce(application_id, '') 聚合
- 分区字段：无
- 推荐项目：`dmp_ads`（bi/ads）或 `dmp_cdm`（dwd/dim）

## 关键字段

`dt`、`revenue_type`、`time_grain`、`paid_amount`

## 字段

| 字段 | 类型 | 说明 | 来源 |
|---|---|---|---|
| `dt` | `STRING` | 日期：天,周,月 | ddl |
| `application_name` | `STRING` | 终端：PC站,M站,全部 | ddl |
| `revenue_type` | `STRING` | 营收类型：轻享会员,高级会员,即时支付会员 | ddl |
| `time_grain` | `STRING` | 统计粒度：日,周,月 | ddl |
| `paid_amount` | `decimal(20,2` | 未提供字段注释 | ddl |

## ETL 与查询提示

- 写入方式：overwrite
- 上游表：`dwd.dwd_zxxk_zxxk_trd_b_plusorder_df`, `dwd.dwd_zxxk_zxxk_trd_cl_payment_df`, `dwd.dwd_zxxk_zxxk_log_consume_log_di`, `bi.bi_zxxk_zxxk_mbr_user_paid_1d`, `t1_final_result`, `t3_final_result`, `t2_1week_ago_final_result`, `t2_thisweek_final_result`
- 过滤条件：(to_date(pay_time) = '${dt}' or add_months(cast(to_date(pay_time) as string), 12) = '${dt}' or add_months(cast(to_date(pay_time) as string), 24) = '${dt}' ) and product in (0, 1, 5, 8, 9, 10) and pay_status = 1 and product_type = 4；(to_date(pay_time) = '${dt}' or add_months(cast(to_date(pay_time) as string), 12) = '${dt}' or add_months(cast(to_date(pay_time) as string), 24) = '${dt}' ) and product in (0, 1, 5, 8, 9, 10) and `status` = 1；(to_date(dt) = '${dt}' or add_months(cast(to_date(dt) as string), 12) = '${dt}' or add_months(cast(to_date(dt) as string), 24) = '${dt}' ) and consume_type = 5 and resource_level != 3；(to_date(dt) = '${dt}' or add_months(cast(to_date(dt) as string), 12) = '${dt}' or add_months(cast(to_date(dt) as string), 24) = '${dt}' ) and consume_type = 5 and resource_level != 3 ) A ), t3_final_result as ( -----time_grain='月'下：这个月初到dt最新的全量结果数据。 select dt, case when application_id = 'pczhan' then 'PC站' when application_id = 'mzhan' then 'M站' when application_id = 'androidapp' then '安卓app' when application_id = 'iosapp' then 'iOSapp' when application_id = 'weixinxiaochengxu' then '微信小程序' when application_id = 'app' then 'App' when application_id = 'other' then '其他' when application_id = 'quanbu' then '全部' else '' end as application_name, revenue_type, time_grain, paid_amount, paid_amount_last_1year, paid_amount_last_2year, paid_user_cnt, paid_user_cnt_last_1year, paid_user_cnt_last_2year from ( select max(concat(substring('${dt}', 1, 7), '-01')) as dt, case when product = 0 then 'other' when product = 1 then 'pczhan' when product = 5 then 'mzhan' when product = 8 then 'weixinxiaochengxu' when product = 9 then 'androidapp' when product = 10 then 'iosapp' end as application_id, max('轻享会员') as revenue_type, max('月') as time_grain, sum(if(substring(cast(to_date(pay_time) as string), 1, 7) = substring('${dt}', 1, 7), true_price, 0)) as paid_amount, sum(if(substring(cast(add_months(cast(to_date(pay_time) as string), 12) as string), 1, 7) = substring('${dt}', 1, 7), true_price, 0)) as paid_amount_last_1year, sum(if(substring(cast(add_months(cast(to_date(pay_time) as string), 24) as string), 1, 7) = substring('${dt}', 1, 7), true_price, 0)) as paid_amount_last_2year, count(distinct if(substring(cast(to_date(pay_time) as string), 1, 7) = substring('${dt}', 1, 7), user_id, null)) as paid_user_cnt, count(distinct if( substring(cast(add_months(cast(to_date(pay_time) as string), 12) as string), 1, 7) = substring('${dt}', 1, 7), user_id, null)) as paid_user_cnt_last_1year, count(distinct if( substring(cast(add_months(cast(to_date(pay_time) as string), 24) as string), 1, 7) = substring('${dt}', 1, 7), user_id, null)) as paid_user_cnt_last_2year from ${dwd}.dwd_zxxk_zxxk_trd_b_plusorder_df where (substring(cast(to_date(pay_time) as string), 1, 7) = substring('${dt}', 1, 7) or substring(cast(add_months(cast(to_date(pay_time) as string), 12) as string), 1, 7) = substring('${dt}', 1, 7) or substring(cast(add_months(cast(to_date(pay_time) as string), 24) as string), 1, 7) = substring('${dt}', 1, 7) ) and product in (0, 1, 5, 8, 9, 10) and pay_status = 1 and product_type = 4；(substring(cast(to_date(pay_time) as string), 1, 7) = substring('${dt}', 1, 7) or substring(cast(add_months(cast(to_date(pay_time) as string), 12) as string), 1, 7) = substring('${dt}', 1, 7) or substring(cast(add_months(cast(to_date(pay_time) as string), 24) as string), 1, 7) = substring('${dt}', 1, 7) ) and product in (0, 1, 5, 8, 9, 10) and pay_status = 1 and product_type = 4；(substring(cast(to_date(pay_time) as string), 1, 7) = substring('${dt}', 1, 7) or substring(cast(add_months(cast(to_date(pay_time) as string), 12) as string), 1, 7) = substring('${dt}', 1, 7) or substring(cast(add_months(cast(to_date(pay_time) as string), 24) as string), 1, 7) = substring('${dt}', 1, 7) ) and product in (0, 1, 5, 8, 9, 10) and `status` = 1；(substring(cast(to_date(dt) as string), 1, 7) = substring('${dt}', 1, 7) or substring(cast(add_months(cast(to_date(dt) as string), 12) as string), 1, 7) = substring('${dt}', 1, 7) or substring(cast(add_months(cast(to_date(dt) as string), 24) as string), 1, 7) = substring('${dt}', 1, 7) ) and consume_type = 5 and resource_level != 3；(substring(cast(to_date(dt) as string), 1, 7) = substring('${dt}', 1, 7) or substring(cast(add_months(cast(to_date(dt) as string), 12) as string), 1, 7) = substring('${dt}', 1, 7) or substring(cast(add_months(cast(to_date(dt) as string), 24) as string), 1, 7) = substring('${dt}', 1, 7) ) and consume_type = 5 and resource_level != 3 ) A ), t2_1week_ago_final_result as ( -----dt对应的这周一之前的数据，time_grain = '周'的数据 select * from ${bi}.bi_zxxk_zxxk_mbr_user_paid_1d where time_grain = '周' and dt < substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10) ), t2_thisweek_final_result as ( ------dt对应的这周. (从这周一到昨天的数据) select dt, case when application_id = 'pczhan' then 'PC站' when application_id = 'mzhan' then 'M站' when application_id = 'androidapp' then '安卓app' when application_id = 'iosapp' then 'iOSapp' when application_id = 'weixinxiaochengxu' then '微信小程序' when application_id = 'app' then 'App' when application_id = 'other' then '其他' when application_id = 'quanbu' then '全部' else '' end as application_name, revenue_type, time_grain, paid_amount, paid_amount_last_1year, paid_amount_last_2year, paid_user_cnt, paid_user_cnt_last_1year, paid_user_cnt_last_2year from ( select max(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10)) as dt, ----'dt对应的这周的周一' as dt, 为周一 case when product = 0 then 'other' when product = 1 then 'pczhan' when product = 5 then 'mzhan' when product = 8 then 'weixinxiaochengxu' when product = 9 then 'androidapp' when product = 10 then 'iosapp' end as application_id, max('轻享会员') as revenue_type, max('周') as time_grain, sum(if(substring(pay_time, 1, 10) >= substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10) and substring(pay_time, 1, 10) <= '${dt}', true_price, 0)) as paid_amount, sum(if(substring(pay_time, 1, 10) >= substring(cast(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -12) as string), 1, 10) and substring(pay_time, 1, 10) < substring(cast(date_add(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1,10), -12), 7) as string), 1, 10), true_price, 0)) as paid_amount_last_1year, sum(if(substring(pay_time, 1, 10) >= substring(cast(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -24) as string), 1, 10) and substring(pay_time, 1, 10) < substring(cast(date_add(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -24), 7) as string), 1, 10), true_price, 0)) as paid_amount_last_2year, count(distinct if(substring(pay_time, 1, 10) >= substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10) and substring(pay_time, 1, 10) <= '${dt}', user_id, null)) as paid_user_cnt, count(distinct if(substring(pay_time, 1, 10) >= substring(cast(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -12) as string), 1, 10) and substring(pay_time, 1, 10) < substring(cast(date_add(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -12), 7) as string), 1, 10), user_id, null)) as paid_user_cnt_last_1year, count(distinct if(substring(pay_time, 1, 10) >= substring(cast(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -24) as string), 1, 10) and substring(pay_time, 1, 10) < substring(cast(date_add(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -24), 7) as string), 1, 10), user_id, null)) as paid_user_cnt_last_2year from ${dwd}.dwd_zxxk_zxxk_trd_b_plusorder_df where 1 = 1 and ((substring(pay_time, 1, 10) >= substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10) and substring(pay_time, 1, 10) <= '${dt}') or (substring(pay_time, 1, 10) >= substring(cast(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -12) as string), 1, 10) and substring(pay_time, 1, 10) < substring(cast(date_add(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -12), 7) as string), 1, 10)) or (substring(pay_time, 1, 10) >= substring(cast(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -24) as string), 1, 10) and substring(pay_time, 1, 10) < substring(cast(date_add(add_months(substring(cast(date_sub(substring('${dt}', 1, 10), pmod(datediff(substring('${dt}', 1, 10), '1900-01-01'), 7)) as string), 1, 10), -24), 7) as string), 1, 10))) and product in (0, 1, 5, 8, 9, 10) and pay_status = 1 and product_type = 4
- 聚合函数：MAX('${dt}'), MAX('轻享会员'), MAX('天'), SUM(if(to_date(pay_time), SUM(if(add_months(cast(to_date(pay_time), COUNT(distinct if(to_date(pay_time), COUNT(distinct if(add_months(cast(to_date(pay_time), MAX('高级会员'), MAX('即时支付'), SUM(if(to_date(dt), SUM(if(add_months(cast(to_date(dt), COUNT(distinct if(to_date(dt)

## 使用边界

- 本文件是基于本地 DDL/DML 的辅助元数据，不替代 MaxCompute 实时 schema。
- 执行 SQL 前使用 `mcp__maxcompute__get_table_schema` 核对字段、类型和分区。
- 查询分区表必须在 WHERE 中添加分区过滤，避免全表扫描。

## 数据质量提示

- 字段 paid_amount 缺少注释
- 未匹配到 INSERT SQL，来源与指标逻辑待补充
