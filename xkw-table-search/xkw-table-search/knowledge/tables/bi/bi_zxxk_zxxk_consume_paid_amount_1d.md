# `bi_zxxk_zxxk_consume_paid_amount_1d`

- 层级：`bi`
- 本地表描述：日期
- 主题标签：log_behavior
- 数据粒度：按 application_id with rollup ), dl_user as ( select dt,consumer_id AS user_id,application_id from ${dwd}.dwd_zxxk_zxxk_log_consume_log_di where ((dt >= DATETRUNC(TO_DATE('${dt}'), 'MM') and dt < add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), 1)) or (dt >= add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'),-12) and dt < add_months(add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), 1),-12)) or (dt >= add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'),-24) and dt < add_months(add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), 1),-24))) and coalesce(resource_type,0) not in (3,5) and coalesce(product,0)<>9 and consume_type in (1,2,3,5,6,520,521,541) group by dt,consumer_id,application_id ), mid_zxxk_dl_user_rlt as ( select coalesce(application_id, '全部') application_id, count(distinct case when dt = '${dt}' then user_id end) as dl_user_cnt_day, count(distinct case when dt = add_months('${dt}', -12) then user_id end) as dl_user_cnt_day_1year, count(distinct case when dt = add_months('${dt}', -24) then user_id end) as dl_user_cnt_day_2year, count(distinct case when dt >= date_sub(next_day('${dt}', 'MONDAY'), 7) and dt <= '${dt}' then user_id end) as dl_user_cnt_wk, count(distinct case when dt >= add_months(date_sub(next_day('${dt}', 'MONDAY'), 7), -12) and dt <= add_months('${dt}', -12) then user_id end) as dl_user_cnt_wk_1year, count(distinct case when dt >= add_months(date_sub(next_day('${dt}', 'MONDAY'), 7), -24) and dt <= add_months('${dt}', -24) then user_id end) as dl_user_cnt_wk_2year, count(distinct case when dt >= DATETRUNC(TO_DATE('${dt}'), 'MM') and dt <= '${dt}' then user_id end) as dl_user_cnt_mth, count(distinct case when dt >= add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), -12) and dt <= add_months('${dt}', -12) then user_id end) as dl_user_cnt_mth_1year, count(distinct case when dt >= add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), -24) and dt <= add_months('${dt}', -24) then user_id end) as dl_user_cnt_mth_2year from dl_user w group by application_id with rollup ), dim as ( select '${dt}' dt,'pczhan' application_id,'PC站' application_name 聚合
- 分区字段：p_dt
- 推荐项目：`dmp_ads`（bi/ads）或 `dmp_cdm`（dwd/dim）

## 关键字段

`dt`、`time_grain`、`consume_amount`、`p_dt`

## 字段

| 字段 | 类型 | 说明 | 来源 |
|---|---|---|---|
| `dt` | `string` | 日期 | ddl |
| `application_name` | `string` | 终端 转成中文，例如PC站，M站，APP，其他, 全部 | ddl |
| `time_grain` | `string` | 统计粒度 日/周/月 | ddl |
| `consume_amount` | `decimal(20,2` | 未提供字段注释 | ddl |
| `p_dt` | `string` | 天分区 | ddl / 分区 |

## ETL 与查询提示

- 写入方式：overwrite
- 上游表：`bi.bi_zxxk_zxxk_user_action_1d`, `dwd.dwd_pub_io_log_xyiolog_miniprogram_di`, `active_user`, `dwd.dwd_zxxk_zxxk_log_consume_log_di`, `dl_user`, `bi.bi_zxxk_zxxk_consume_paid_amount_1d`, `dim`, `mid_zxxk_user_rlt`, `mid_zxxk_dl_user_rlt`
- 关联条件：a.application_id=b.application_id
         left；a.application_id=b1.application_id；a.application_id=b1.application_id
;
- 过滤条件：(dt >= DATETRUNC(TO_DATE('${dt}'), 'MM') and dt < add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), 1)) or (dt >= add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'),-12) and dt < add_months(add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), 1),-12)) or (dt >= add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'),-24) and dt < add_months(add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), 1),-24))；product_id='xuekewang' and application_id='weixinxiaochengxu' and log_event_type='view' and user_id<>0 and user_id is not null and is_tob=0 and ((dt >= DATETRUNC(TO_DATE('${dt}'), 'MM') and dt < add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), 1)) or (dt >= add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'),-12) and dt < add_months(add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), 1),-12)) or (dt >= add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'),-24) and dt < add_months(add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), 1),-24))) ), mid_zxxk_user_rlt as ( select coalesce(application_id, '全部') application_id, count(distinct case when dt = '${dt}' and c_active = 1 then user_id end) as c_active_user_cnt_day, count(distinct case when dt = add_months('${dt}', -12) and c_active = 1 then user_id end) as c_active_user_cnt_day_1year, count(distinct case when dt = add_months('${dt}', -24) and c_active = 1 then user_id end) as c_active_user_cnt_day_2year, count(distinct case when dt >= date_sub(next_day('${dt}', 'MONDAY'), 7) and dt <= '${dt}' and c_active = 1 then user_id end) as c_active_user_cnt_wk, count(distinct case when dt >= add_months(date_sub(next_day('${dt}', 'MONDAY'), 7), -12) and dt <= add_months('${dt}', -12) and c_active = 1 then user_id end) as c_active_user_cnt_wk_1year, count(distinct case when dt >= add_months(date_sub(next_day('${dt}', 'MONDAY'), 7), -24) and dt <= add_months('${dt}', -24) and c_active = 1 then user_id end) as c_active_user_cnt_wk_2year, count(distinct case when dt >= DATETRUNC(TO_DATE('${dt}'), 'MM') and dt <= '${dt}' and c_active = 1 then user_id end) as c_active_user_cnt_mth, count(distinct case when dt >= add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), -12) and dt <= add_months('${dt}', -12) and c_active = 1 then user_id end) as c_active_user_cnt_mth_1year, count(distinct case when dt >= add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), -24) and dt <= add_months('${dt}', -24) and c_active = 1 then user_id end) as c_active_user_cnt_mth_2year, count(distinct case when dt = '${dt}' and user_reg_dt >= '${dt}' and c_active = 1 then user_id end) as reg_c_active_user_cnt_day, count(distinct case when dt = add_months('${dt}', -12) and user_reg_dt >= add_months('${dt}', -12) and c_active = 1 then user_id end) as reg_c_active_user_cnt_day_1year, count(distinct case when dt = add_months('${dt}', -24) and user_reg_dt >= add_months('${dt}', -24) and c_active = 1 then user_id end) as reg_c_active_user_cnt_day_2year, count(distinct case when dt >= date_sub(next_day('${dt}', 'MONDAY'), 7) and dt <= '${dt}' and user_reg_dt >= date_sub(next_day('${dt}', 'MONDAY'), 7) and c_active = 1 then user_id end) as reg_c_active_user_cnt_wk, count(distinct case when dt >= add_months(date_sub(next_day('${dt}', 'MONDAY'), 7), -12) and dt <= add_months('${dt}', -12) and user_reg_dt >= add_months(date_sub(next_day('${dt}', 'MONDAY'), 7), -12) and c_active = 1 then user_id end) as reg_c_active_user_cnt_wk_1year, count(distinct case when dt >= add_months(date_sub(next_day('${dt}', 'MONDAY'), 7), -24) and dt <= add_months('${dt}', -24) and user_reg_dt >= add_months(date_sub(next_day('${dt}', 'MONDAY'), 7), -24) and c_active = 1 then user_id end) as reg_c_active_user_cnt_wk_2year, count(distinct case when dt >= DATETRUNC(TO_DATE('${dt}'), 'MM') and dt <= '${dt}' and user_reg_dt >= DATETRUNC(TO_DATE('${dt}'), 'MM') and c_active = 1 then user_id end) as reg_c_active_user_cnt_mth, count(distinct case when dt >= add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), -12) and dt <= add_months('${dt}', -12) and user_reg_dt >= add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), -12) and c_active = 1 then user_id end) as reg_c_active_user_cnt_mth_1year, count(distinct case when dt >= add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), -24) and dt <= add_months('${dt}', -24) and user_reg_dt >= add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), -24) and c_active = 1 then user_id end) as reg_c_active_user_cnt_mth_2year, sum(case when dt = '${dt}' then dl_consume_amount END) as dl_amount_day, sum( case when dt = add_months('${dt}', -12) then dl_consume_amount END) as dl_amount_day_1year, sum( case when dt = add_months('${dt}', -24) then dl_consume_amount END) as dl_amount_day_2year, sum(case when dt >= date_sub(next_day('${dt}', 'MONDAY'), 7) and dt <= '${dt}' then dl_consume_amount END) as dl_amount_wk, sum(case when dt >= add_months(date_sub(next_day('${dt}', 'MONDAY'), 7), -12) and dt <= add_months('${dt}', -12) then dl_consume_amount END) as dl_amount_wk_1year, sum(case when dt >= add_months(date_sub(next_day('${dt}', 'MONDAY'), 7), -24) and dt <= add_months('${dt}', -24) then dl_consume_amount END) as dl_amount_wk_2year, sum(case when dt >= DATETRUNC(TO_DATE('${dt}'), 'MM') and dt <= '${dt}' then dl_consume_amount END) as dl_amount_mth, sum(case when dt >= add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), -12) and dt <= add_months('${dt}', -12) then dl_consume_amount END) as dl_amount_mth_1year, sum(case when dt >= add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), -24) and dt <= add_months('${dt}', -24) then dl_consume_amount END) as dl_amount_mth_2year, count(distinct case when dt = '${dt}' and paid_amount > 0 then user_id end) as paid_user_cnt_day, count(distinct case when dt = add_months('${dt}', -12) and paid_amount > 0 then user_id end) as paid_user_cnt_day_1year, count(distinct case when dt = add_months('${dt}', -24) and paid_amount > 0 then user_id end) as paid_user_cnt_day_2year, count(distinct case when dt >= date_sub(next_day('${dt}', 'MONDAY'), 7) and dt <= '${dt}' and paid_amount > 0 then user_id end) as paid_user_cnt_wk, count(distinct case when dt >= add_months(date_sub(next_day('${dt}', 'MONDAY'), 7), -12) and dt <= add_months('${dt}', -12) and paid_amount > 0 then user_id end) as paid_user_cnt_wk_1year, count(distinct case when dt >= add_months(date_sub(next_day('${dt}', 'MONDAY'), 7), -24) and dt <= add_months('${dt}', -24) and paid_amount > 0 then user_id end) as paid_user_cnt_wk_2year, count(distinct case when dt >= DATETRUNC(TO_DATE('${dt}'), 'MM') and dt <= '${dt}' and paid_amount > 0 then user_id end) as paid_user_cnt_mth, count(distinct case when dt >= add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), -12) and dt <= add_months('${dt}', -12) and paid_amount > 0 then user_id end) as paid_user_cnt_mth_1year, count(distinct case when dt >= add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), -24) and dt <= add_months('${dt}', -24) and paid_amount > 0 then user_id end) as paid_user_cnt_mth_2year, sum(case when dt = '${dt}' then paid_amount END) as paid_amount_day, sum(case when dt = add_months('${dt}', -12) then paid_amount END) as paid_amount_day_1year, sum(case when dt = add_months('${dt}', -24) then paid_amount END) as paid_amount_day_2year, sum(case when dt >= date_sub(next_day('${dt}', 'MONDAY'), 7) and dt <= '${dt}' then paid_amount END) as paid_amount_wk, sum(case when dt >= add_months(date_sub(next_day('${dt}', 'MONDAY'), 7), -12) and dt <= add_months('${dt}', -12) then paid_amount END) as paid_amount_wk_1year, sum(case when dt >= add_months(date_sub(next_day('${dt}', 'MONDAY'), 7), -24) and dt <= add_months('${dt}', -24) then paid_amount END) as paid_amount_wk_2year, sum(case when dt >= DATETRUNC(TO_DATE('${dt}'), 'MM') and dt <= '${dt}' then paid_amount END) as paid_amount_mth , sum(case when dt >= add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), -12) and dt <= add_months('${dt}', -12) then paid_amount END) as paid_amount_mth_1year , sum(case when dt >= add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), -24) and dt <= add_months('${dt}', -24) then paid_amount END) as paid_amount_mth_2year from active_user w；((dt >= DATETRUNC(TO_DATE('${dt}'), 'MM') and dt < add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), 1)) or (dt >= add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'),-12) and dt < add_months(add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), 1),-12)) or (dt >= add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'),-24) and dt < add_months(add_months(DATETRUNC(TO_DATE('${dt}'), 'MM'), 1),-24))) and coalesce(resource_type,0) not in (3,5) and coalesce(product,0)<>9 and consume_type in (1,2,3,5,6,520,521,541)；1=1 and p_dt='${dt}' and case when time_grain='天' and dt='${dt}' then 1 when time_grain='周' and date_sub(next_day('${dt}','MONDAY'),7)=dt then 1 when time_grain='月' and DATETRUNC(TO_DATE('${dt}'), 'MM')=dt then 1 else 0 end=0
- 聚合函数：COUNT(distinct case when dt = '${dt}' and c_active = 1 then user_id end), COUNT(distinct
                 case when dt = add_months('${dt}', -12), COUNT(distinct
                 case when dt = add_months('${dt}', -24), COUNT(distinct case
                              when dt >= date_sub(next_day('${dt}', 'MONDAY'), COUNT(distinct case
                              when dt >= add_months(date_sub(next_day('${dt}', 'MONDAY'), COUNT(distinct case
                              when dt >= DATETRUNC(TO_DATE('${dt}'), COUNT(distinct case
                              when dt >= add_months(DATETRUNC(TO_DATE('${dt}'), COUNT(distinct case
                              when dt = '${dt}' and user_reg_dt >= '${dt}' and c_active = 1
                                  then user_id end), COUNT(distinct case
                              when dt = add_months('${dt}', -12), COUNT(distinct case
                              when dt = add_months('${dt}', -24), SUM(case when dt = '${dt}' then dl_consume_amount END), SUM(case when dt = add_months('${dt}', -12)

## 使用边界

- 本文件是基于本地 DDL/DML 的辅助元数据，不替代 MaxCompute 实时 schema。
- 执行 SQL 前使用 `mcp__maxcompute__get_table_schema` 核对字段、类型和分区。
- 查询分区表必须在 WHERE 中添加分区过滤，避免全表扫描。

## 数据质量提示

- 字段 consume_amount 缺少注释
- 未匹配到 INSERT SQL，来源与指标逻辑待补充
