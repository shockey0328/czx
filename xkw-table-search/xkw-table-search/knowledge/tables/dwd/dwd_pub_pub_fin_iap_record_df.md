# `dwd_pub_pub_fin_iap_record_df`

- 层级：`dwd`
- 本地表描述：主键ID
- 主题标签：transaction_payment, finance
- 数据粒度：需结合实时 schema 与业务口径确认
- 分区字段：无
- 推荐项目：`dmp_ads`（bi/ads）或 `dmp_cdm`（dwd/dim）

## 关键字段

`id`、`user_id`、`product_id`、`fin_io_type`

## 字段

| 字段 | 类型 | 说明 | 来源 |
|---|---|---|---|
| `id` | `INT` | 主键ID | ddl |
| `user_id` | `INT` | 用户ID | ddl |
| `product_id` | `STRING` | 产品ID | ddl |
| `fin_io_direction` | `STRING` | (财务口径)收支方向: i代表收入,o代表支出 | ddl |
| `fin_io_type` | `INT` | 收支类型(财务口径) | ddl |
| `fin_io_actual` | `DECIMAL(20,2` | 未提供字段注释 | ddl |

## ETL 与查询提示

- 写入方式：overwrite
- 上游表：`dwd.dwd_ump_uc_trd_iap_record_df`, `temp_input`, `dwd.dwd_ump_pay_trd_trade_wide_di`, `dim.dim_pub_pub_pay_channel`, `dim.dim_pub_pub_fin_io_type`, `temp_zxxkmobile_type_id2_changeflag1`, `temp_zxxkmobile_type_id1_changeflag1`, `result_input`, `temp_zj_app_type_id1_changeflag1_result`, `temp_zxxkmobile_type_id2_changeflag1_result`, `temp_zxxkmobile_type_id1_changeflag1_result`, `temp_output`, `temp_zxxkmobile_type_id43_changeflag0_result`
- 关联条件：paid_di.channel_id = pay_channel.channel_id
                  left；paid_di.channel_id = pay_channel.channel_id
                                                       left
- 过滤条件：1=1 and ((source_app = 'yfsb' and type_id = 2 and change_flag = 0) or (source_app = 'zj.app' and type_id = 2 and change_flag = 0) or (source_app = 'zxxkmobile' and type_id = 2 and change_flag = 0) or (source_app = 'zxxkmobile' and type_id = 42 and change_flag = 0) ) ), result_input as ( ----1.(财务口径)收支方向为收入的result表 (2023-08-08号有数据) select temp_input.id, temp_input.user_id, temp_input.product_id, temp_input.fin_io_direction, nvl(fin_io_type_tbl.io_type_id, 15) as fin_io_type, ----收支类型(财务口径)，该类型的即为收入来源， 15代表其他 cast(paid_change as decimal(20,2)) as fin_io_actual, ----实付金额(财务口径) cast(paid_before as decimal(20,2)) as fin_io_actual_before, ----实付前金额(财务口径) cast(paid_after as decimal(20,2)) as fin_io_actual_after, ----实付后金额 temp_input.transaction_no, ----交易流水号 temp_input.remark, temp_input.operator_id, ----操作人ID '' as order_no, ----订单号 fin_io_time, temp_input.dt as dt from temp_input left join ${dwd}.dwd_ump_pay_trd_trade_wide_di paid_di ------等同于dwd.dwd_uc_newpay_charges表 on paid_di.dt>='2000-01-01' and temp_input.charge_id = CONCAT('ch_20',paid_di.trade_no) left join ${dim}.dim_pub_pub_pay_channel pay_channel on paid_di.channel_id = pay_channel.channel_id left join (select * from ${dim}.dim_pub_pub_fin_io_type where is_iap = 1 )fin_io_type_tbl on pay_channel.fin_payment_source = fin_io_type_tbl.io_type_name ), temp_zj_app_type_id1_changeflag1_result as( -----2.(source_app='zj.app' and type_id = 1 and change_flag =1) result表. (2023-03-20号有数据) select id, user_id, product_id, 'i' as fin_io_direction, 12 as fin_io_type, -----收支类型：12代表APPLE账户 cast(0 - paid_change as decimal(20,2)) as fin_io_actual, ----实付金额(财务口径), 设为负数 cast(paid_before as decimal(20,2)) as fin_io_actual_before, ----实付前金额(财务口径) cast(paid_after as decimal(20,2)) as fin_io_actual_after, transaction_no, remark, operator operator_id, '' as order_no, substring(add_time, 1, 19) as fin_io_time, dt from ${dwd}.dwd_ump_uc_trd_iap_record_df where 1=1 and (source_app = 'zj.app' and type_id = 41 and change_flag = 1) ), temp_zxxkmobile_type_id2_changeflag1 as( -----source_app = 'zxxkmobile' and type_id =2 and change_flag = 1, 单独拆一个表 去关联new_pay_charge select id, user_id, product_id, transaction_no, remark, operator operator_id, get_json_object(remark, "$.orderNo") as order_no, add_time, paid_before, paid_after, paid_change, dt from ${dwd}.dwd_ump_uc_trd_iap_record_df where 1=1 and (source_app = 'zxxkmobile' and type_id = 2 and change_flag = 1) ), temp_zxxkmobile_type_id2_changeflag1_result as ( ------3.source_app = 'zxxkmobile' and type_id =2 and change_flag = 1的 result表. (2023-06-14号有该类型数据) select iap.id, iap.user_id, iap.product_id, case when paid_di.trade_no is null then 'o' ---关联不上 when paid_di.trade_no is not null then 'i' ---可以关联上 end as fin_io_direction, ----（财务口径）收支方向：收入，支出 case when paid_di.trade_no is null then 10 -----10 代表人工处理 when paid_di.trade_no is not null then fin_io_type_tbl.io_type_id end as fin_io_type, -----收支类型 case when paid_di.trade_no is null then cast(paid_change as decimal(20,2)) when paid_di.trade_no is not null then cast(0 - paid_change as decimal(20,2)) end as fin_io_actual, ----实付金额(财务口径) cast(paid_before as decimal(20, 2)) as fin_io_actual_before, ----实付前金额(财务口径) cast(paid_after as decimal(20, 2)) as fin_io_actual_after, ----实付后金额(财务口径) iap.transaction_no as transaction_no, iap.remark as remark, iap.operator_id as operator_id, iap.order_no as order_no, substring(iap.add_time, 1, 19) as fin_io_time, iap.dt as dt from temp_zxxkmobile_type_id2_changeflag1 iap left join ${dwd}.dwd_ump_pay_trd_trade_wide_di paid_di ------等同于dwd.dwd_uc_newpay_charges表 on paid_di.dt>='2000-01-01' and iap.order_no = paid_di.app_sub_trade_no left join ${dim}.dim_pub_pub_pay_channel pay_channel on paid_di.channel_id = pay_channel.channel_id left join (select * from ${dim}.dim_pub_pub_fin_io_type where is_iap = 1 ) fin_io_type_tbl on pay_channel.fin_payment_source = fin_io_type_tbl.io_type_name ), temp_zxxkmobile_type_id1_changeflag1 as ( ----4.特殊处理id=469434的记录 select id, user_id, product_id, transaction_no, remark, operator operator_id, case when id=1851273 then 'Y2025041917395204033207321' else split(remark, '订单号：')[1] end as order_no, add_time, paid_before, paid_after, paid_change, dt from ${dwd}.dwd_ump_uc_trd_iap_record_df where id in (469434,1851273) ), temp_zxxkmobile_type_id1_changeflag1_result as( select id, user_id, product_id, 'i' as fin_io_direction, 12 as fin_io_type, -----收支类型, 12 APP账户 cast(0 - paid_change as decimal(20,2)) as fin_io_actual, -----实付金额 cast(paid_before as decimal(20, 2)) as fin_io_actual_before, ----实付前金额(财务口径) cast(paid_after as decimal(20, 2)) as fin_io_actual_after, ----实付后金额(财务口径) transaction_no, remark, operator_id, order_no, substring(add_time, 1, 19) as fin_io_time, dt from temp_zxxkmobile_type_id1_changeflag1 ), temp_output as ( ----5. (财务口径)收支方向为支出(即1)的 result. (2023-06-14号有数据, 2023-08-08号有数据) select id, user_id, product_id, 'o' as fin_io_direction, case when source_app = 'yfsb' and type_id = 1 and change_flag = 1 then 7 -----7:'资源下载' when source_app = 'yfsb' and type_id = 40 and change_flag = 1 and remark like '%AI研修会员产品%' then 9 -----9:'资产转换(学贝会员)' when source_app = 'yfsb' and type_id = 40 and change_flag = 1 and remark like '%AI研修课程资源产品%' then 7 -----7:'资源下载' when source_app = 'zj.app' and type_id = 1 and change_flag = 1 then 7 -----7:'资源下载' when source_app = 'zj.app' and type_id = 2 and change_flag = 1 then 8 -----8:'资产转换(学贝储值)' when source_app = 'zj.app' and type_id = 40 and change_flag = 1 then 9 -----9:'资产转换(学贝会员)' when source_app = 'zj.app' and type_id = 41 and change_flag = 0 and remark like '%学贝%会员%失败自动退回%' then 9 when source_app = 'zj.app' and type_id = 41 and change_flag = 0 then 7 -----7:'资源下载' when source_app = 'zy.paycenter' and type_id = 1 and change_flag = 1 then 7 -----7:'资源下载' when source_app = 'zy.paycenter' and type_id = 40 and change_flag = 1 and remark like '%高级会员%' then 8 ------8:'资产转换（学贝储值）' when source_app = 'zy.paycenter' and type_id = 40 and change_flag = 1 and remark like '%包月会员%' then 9 ------9:'资产转换（学贝会员）' when source_app = 'zy.paycenter' and type_id = 45 and change_flag = 1 then 8 -----8:'资产转换（学贝储值）' when source_app = 'zy.paycenter' and type_id = 46 and change_flag = 1 then 9 -----9:'资产转换（学贝会员）' end as fin_io_type, -----收支类型(财务口径) case when source_app = 'zj.app' and type_id = 41 and change_flag = 0 then cast(0 - paid_change as decimal(20,2)) else cast(paid_change as decimal(20,2)) end as fin_io_actual, -----实付金额(财务口径) cast(paid_before as decimal(20,2)) as fin_io_actual_before, -----实付前金额（财务口径） cast(paid_after as decimal(20,2)) as fin_io_actual_after, -----实付后金额（财务口径）, transaction_no, remark, operator operator_id, case when source_app = 'zy.paycenter' and type_id in (1,40,45,46) then get_json_object(remark, "$.orderNo") else '' end as order_no, -----订单号 substring(add_time,1,19) as fin_io_time, dt from ${dwd}.dwd_ump_uc_trd_iap_record_df where 1=1 and ((source_app = 'yfsb' and type_id = 1 and change_flag = 1) or (source_app = 'yfsb' and type_id = 40 and change_flag = 1) or (source_app = 'zj.app' and type_id = 1 and change_flag = 1) or (source_app = 'zj.app' and type_id = 2 and change_flag = 1) or (source_app = 'zj.app' and type_id = 40 and change_flag = 1) or (source_app = 'zj.app' and type_id = 41 and change_flag = 0) or (source_app = 'zy.paycenter' and type_id = 1 and change_flag = 1) or (source_app = 'zy.paycenter' and type_id = 40 and change_flag = 1) or (source_app = 'zy.paycenter' and type_id = 45 and change_flag = 1) or (source_app = 'zy.paycenter' and type_id = 46 and change_flag = 1) ) ), temp_zxxkmobile_type_id43_changeflag0_result as ( ----source_app=zxxkmobile,type_id=43的数据处理 select id, user_id, 'xuekewang' as product_id, ---产品id 'i' as fin_io_direction, ---收支方向 15 as fin_io_type, ---收支类型: 15其他 cast(paid_change + gift_change as decimal(20,2)) as fin_io_actual, ---实付金额 total_before as fin_io_actual_before, ---实付前金额 total_after as fin_io_actual_after, ---实付后金额 transaction_no, remark, operator operator_id, '' as order_no, ---订单号 substring(add_time,1, 19) as fin_io_time, ---订单时间 dt ---订单日期 from ${dwd}.dwd_ump_uc_trd_iap_record_df where source_app = 'zxxkmobile' and type_id = 43 and change_flag = 0 )

## 使用边界

- 本文件是基于本地 DDL/DML 的辅助元数据，不替代 MaxCompute 实时 schema。
- 执行 SQL 前使用 `mcp__maxcompute__get_table_schema` 核对字段、类型和分区。
- 查询分区表必须在 WHERE 中添加分区过滤，避免全表扫描。

## 数据质量提示

- 字段 fin_io_actual 缺少注释
- 未匹配到 INSERT SQL，来源与指标逻辑待补充
