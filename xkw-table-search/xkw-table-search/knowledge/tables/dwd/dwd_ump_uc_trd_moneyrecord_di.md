# `dwd_ump_uc_trd_moneyrecord_di`

- 层级：`dwd`
- 本地表描述：储值流水号
- 主题标签：transaction_payment
- 数据粒度：按 SUBSTRING(NVL(CAST(date_format(cast(from_unixtime(unix_timestamp(regexp_replace(regexp_replace(addtime, 'T', ' '), 'Z', ''), 'yyyy-MM-dd HH:mm:ss') + 8*3600) as timestamp), 'yyyy-MM-dd HH:mm:ss') AS STRING),'9999-99-99'),1,10)) and ((source = 'zxxk.download' and typeid = 1 and income <= 0) or (source = 'zxxk.download' and typeid = 39) or (source = 'zxxk.xxedu' and typeid in (1, 8, 39)) or (source = 'zxxk.paycenter' and typeid in (2, 42,47)) or (source = 'zxxk.mobile' and typeid in (2, 8, 29)) or (source = 'zxxk.usercentertemp' and typeid = 2) or (source = 'zy.usercenter' and typeid = 29) or (source in ('zxxk.cms', 'zy.xmp') and typeid = 34) or (source = 'zxxk.paper' and typeid in (0, 1, 2,47)) or (source = 'yfsb' and typeid in (1, 40)) or (source = 'zxxk.jiaopei' and typeid in (1, 40)) or (source in ('jx.ekt', 'zxxk.xyy','zxxk.beike') and typeid = 39) or (source in ('jx.ekt','zxxk.beike') and typeid = 2) or (source in ('zxxk.ucm', 'ucm') and typeid = 34) or (source = 'system' and typeid in (44, 9999)) or (source = 'zxxk.account' and typeid = 8) or (source = 'zxxk.oa' and typeid = 34) or (source = 'zxxk.oauser' and typeid in (2, 34, 44)) or (source is null and typeid in (2, 9999)) ) ), temp1_order as ( ---获取order_no select id, userid, product_id, transaction_no, case when product_id = 'xuekewang' and remark like '通过学科网现金充值储值%， 交易单号为%' then translate(translate(substr(split(remark, '交易单号')[1], 2), '（', ''), '）', '') when product_id = 'zujuanwang' and remark like '组卷扫码充值储值%,订单号%' then translate(translate(substr(split(remark, '订单号')[1], 2), '（', ''), '）', '') when product_id = 'ebeike' and remark like '备课充储值,订单号:%' then translate(translate(substr(split(remark, '订单号')[1], 2), '（', ''), '）', '') when source in('jx.ekt','zxxk.beike') and typeid = 2 and dt>='2024-01-01' and remark like '通过e备课充储值%,订单号:%' then translate(translate(substr(split(remark, '订单号')[1], 2), '（', ''), '）', '') when source='jx.ekt' and typeid=2 and dt>='2024-01-15' then transaction_no when source='zxxk.paper' and typeid=47 and dt>='2023-11-15' then transaction_no when source='zxxk.paycenter' and typeid=47 and dt>='2023-11-15' then transaction_no else '' end as order_no, before_num, income, outlay, after_num, typeid, remark, censorid, censorname, addtime, source, paid_before, paid_after, paid_change, gift_before, gift_after, gift_change, original_order_id, dt from temp1 ), temp_source_zxxkdownload_type_id1_back_tbl as ( ---针对source='zxxkdownload' and type_id=1 and income > 0 的订单特殊处理 select id, userid, 'xuekewang' as product_id, transaction_no, cast(beforenum as decimal(20,2)) as before_num, cast(income as decimal(20,2)) as income, cast(outlay as decimal(20,2)) as outlay, cast(afternum as decimal(20,2)) as after_num, typeid, remark, censorid, censorname, addtime, source, cast(paid_before as decimal(20,2)) as paid_before, cast(paid_after as decimal(20,2)) as paid_after, cast(paid_change as decimal(20,2)) as paid_change, cast(gift_before as decimal(20,2)) as gift_before, cast(gift_after as decimal(20,2)) as gift_after, cast(gift_change as decimal(20,2)) as gift_change, 0 as original_order_id, substr(addtime, 1, 10) as dt from ${ods}.ods_ump_uc_uc_tbl_t_moneyrecord moneyrecord where dt in (SELECT SUBSTRING(NVL(CAST(date_format(cast(from_unixtime(unix_timestamp(regexp_replace(regexp_replace(addtime, 'T', ' '), 'Z', ''), 'yyyy-MM-dd HH:mm:ss') + 8*3600) as timestamp), 'yyyy-MM-dd HH:mm:ss') AS STRING),'9999-99-99'),1,10) FROM ${ods}.stg_dmp_dti_kafka_tbl_dmp_dti_data_change src LATERAL VIEW JSON_TUPLE(after, 'AddTime') lv AS `addtime` WHERE 1 = 1 AND dt >= '${dt}' AND addr = 'rdsx0a60aa3mvdoptj3e915.mysql.rds.aliyuncs.com:3306' AND db = 'uc' AND table = 't_moneyrecord' GROUP BY SUBSTRING(NVL(CAST(date_format(cast(from_unixtime(unix_timestamp(regexp_replace(regexp_replace(addtime, 'T', ' '), 'Z', ''), 'yyyy-MM-dd HH:mm:ss') + 8*3600) as timestamp), 'yyyy-MM-dd HH:mm:ss') AS STRING),'9999-99-99'),1,10) ) and source = 'zxxk.download' and typeid = 1 and income > 0 ), temp_source_zxxkdownload_type_id1_back_result as ( select *, nvl(pre_93.pre_93_id, 0) as original_order_id2 ----需要把原始订单orderid找出,如果没有值 赋为0 from temp_source_zxxkdownload_type_id1_back_tbl back left join (select id as pre_93_id from ${dwd}.dwd_ump_uc_trd_moneyrecord_di where dt >= substring(cast(date_sub('${dt}', 93) as string), 1,10)) pre_93 on back.transaction_no = cast(pre_93.pre_93_id as string) ), temp2 as ( ---这种订单的product_id需要关联原订单的order_no select *, substr(substr(split(remark, '原路退回')[1], 2), 1,length(substr(split(remark, '原路退回')[1], 2))-1) as original_order_no from ${ods}.ods_ump_uc_uc_tbl_t_moneyrecord where dt in (SELECT SUBSTRING(NVL(CAST(date_format(cast(from_unixtime(unix_timestamp(regexp_replace(regexp_replace(addtime, 'T', ' '), 'Z', ''), 'yyyy-MM-dd HH:mm:ss') + 8*3600) as timestamp), 'yyyy-MM-dd HH:mm:ss') AS STRING),'9999-99-99'),1,10) FROM ${ods}.stg_dmp_dti_kafka_tbl_dmp_dti_data_change src LATERAL VIEW JSON_TUPLE(after, 'AddTime') lv AS `addtime` WHERE 1 = 1 AND dt >= '${dt}' AND addr = 'rdsx0a60aa3mvdoptj3e915.mysql.rds.aliyuncs.com:3306' AND db = 'uc' AND table = 't_moneyrecord' GROUP BY SUBSTRING(NVL(CAST(date_format(cast(from_unixtime(unix_timestamp(regexp_replace(regexp_replace(addtime, 'T', ' '), 'Z', ''), 'yyyy-MM-dd HH:mm:ss') + 8*3600) as timestamp), 'yyyy-MM-dd HH:mm:ss') AS STRING),'9999-99-99'),1,10)) and source = 'zxxk.ucm' and typeid in (1, 2, 8) ), temp3 as ( select temp2.id as id, temp2.userid as user_id, record.product_id, temp2.transaction_no, null as order_no, -----temp2.substr(split(remark, '号')[1], 2) as order_no, cast(temp2.beforenum as decimal(20,2)) as before_num, cast(temp2.income as decimal(20,2)) as income, cast(temp2.outlay as decimal(20,2)) as outlay, cast(temp2.afternum as decimal(20,2)) as after_num, temp2.typeid, temp2.remark, temp2.censorid, temp2.censorname, temp2.addtime, temp2.source, cast(temp2.paid_before as decimal(20,2)) as paid_before, cast(temp2.paid_after as decimal(20,2)) as paid_after, cast(temp2.paid_change as decimal(20,2)) as paid_change, cast(temp2.gift_before as decimal(20,2)) as gift_before, cast(temp2.gift_after as decimal(20,2)) as gift_after, cast(temp2.gift_change as decimal(20,2)) as gift_change, record.id as original_order_id, substr(temp2.addtime, 1, 10) as dt from temp2 left join (select id, user_id, product_id, transaction_no, before_num, income, outlay, after_num, type_id, remark, censor_id, censor_name, add_time, source, paid_before, paid_after, paid_change, gift_before, gift_after, gift_change, original_order_id, dt from ${dwd}.dwd_ump_uc_trd_moneyrecord_di where id not in (select id e_id from temp1 group by id) and dt >= '2001-01-01' 聚合
- 分区字段：dt
- 推荐项目：`dmp_ads`（bi/ads）或 `dmp_cdm`（dwd/dim）

## 关键字段

`id`、`user_id`、`product_id`、`before_num`、`dt`

## 字段

| 字段 | 类型 | 说明 | 来源 |
|---|---|---|---|
| `id` | `INT` | 储值流水号 | ddl |
| `user_id` | `INT` | 用户id | ddl |
| `product_id` | `STRING` | 产品id | ddl |
| `transaction_no` | `STRING` | 交易流水号 | ddl |
| `order_no` | `STRING` | 订单号 | ddl |
| `before_num` | `DECIMAL(20,2` | 未提供字段注释 | ddl |
| `dt` | `STRING` | 天分区 | ddl / 分区 |

## ETL 与查询提示

- 写入方式：overwrite
- 上游表：`ods.ods_ump_uc_uc_tbl_t_moneyrecord`, `ods.stg_dmp_dti_kafka_tbl_dmp_dti_data_change`, `temp1`, `temp_source_zxxkdownload_type_id1_back_tbl`, `dwd.dwd_ump_uc_trd_moneyrecord_di`, `temp2`, `temp1_order`, `temp_source_zxxkdownload_type_id1_back_result`, `temp3`
- 过滤条件：dt in (SELECT SUBSTRING(NVL(CAST(date_format(cast(from_unixtime(unix_timestamp(regexp_replace(regexp_replace(addtime, 'T', ' '), 'Z', ''), 'yyyy-MM-dd HH:mm:ss') + 8*3600) as timestamp), 'yyyy-MM-dd HH:mm:ss') AS STRING),'9999-99-99'),1,10) FROM ${ods}.stg_dmp_dti_kafka_tbl_dmp_dti_data_change src LATERAL VIEW JSON_TUPLE(after, 'AddTime') lv AS `addtime` WHERE 1 = 1 AND dt >= '${dt}' AND addr = 'rdsx0a60aa3mvdoptj3e915.mysql.rds.aliyuncs.com:3306' AND db = 'uc' AND table = 't_moneyrecord'；dt >= substring(cast(date_sub('${dt}', 93) as string), 1,10)) pre_93 on back.transaction_no = cast(pre_93.pre_93_id as string) ), temp2 as ( ---这种订单的product_id需要关联原订单的order_no select *, substr(substr(split(remark, '原路退回')[1], 2), 1,length(substr(split(remark, '原路退回')[1], 2))-1) as original_order_no from ${ods}.ods_ump_uc_uc_tbl_t_moneyrecord where dt in (SELECT SUBSTRING(NVL(CAST(date_format(cast(from_unixtime(unix_timestamp(regexp_replace(regexp_replace(addtime, 'T', ' '), 'Z', ''), 'yyyy-MM-dd HH:mm:ss') + 8*3600) as timestamp), 'yyyy-MM-dd HH:mm:ss') AS STRING),'9999-99-99'),1,10) FROM ${ods}.stg_dmp_dti_kafka_tbl_dmp_dti_data_change src LATERAL VIEW JSON_TUPLE(after, 'AddTime') lv AS `addtime` WHERE 1 = 1 AND dt >= '${dt}' AND addr = 'rdsx0a60aa3mvdoptj3e915.mysql.rds.aliyuncs.com:3306' AND db = 'uc' AND table = 't_moneyrecord'；id not in (select id e_id from temp1

## 使用边界

- 本文件是基于本地 DDL/DML 的辅助元数据，不替代 MaxCompute 实时 schema。
- 执行 SQL 前使用 `mcp__maxcompute__get_table_schema` 核对字段、类型和分区。
- 查询分区表必须在 WHERE 中添加分区过滤，避免全表扫描。

## 数据质量提示

- 字段 before_num 缺少注释
- 未匹配到 INSERT SQL，来源与指标逻辑待补充
