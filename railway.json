{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd 用户行为看板（周度） && npm install"
  },
  "deploy": {
    "startCommand": "cd 用户行为看板（周度） && node server-with-db.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
