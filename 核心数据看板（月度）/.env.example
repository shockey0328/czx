# 月度核心数据 — MaxCompute MCP 取数
# 可复制为同目录 .env，或复用仓库根 / 用户行为看板（周度）下的 .env
# 勿将含真实密钥的 .env 提交到 Git

# 与 Cursor mcp.json 中 maxcompute 的 X-MCP-Key 一致
MCP_KEY=

# 可选
MAXCOMPUTE_MCP_URL=https://test-dmp-mcp.xkw.com/maxcompute-mcp
ODPS_MAX_CU=200
# 使用率 SQL 较慢，建议至少 10 分钟
ODPS_WAIT_MS=600000
