# 2026-09-02 工作日志

- 用户行为看板路径分析准确性提升：基于 `dmp_cdm.dwd_pub_io_log_xyiolog_di` DDL，梳理 A 类字段补充需求清单（会话切分 / 页面语义 / 内容ID / 事件类型 / 停留时长 / 接口响应），产出《A类字段补充需求清单.md》供用户评估可补充字段。
- 修正认知：学科/学段/教材版本/权益(toc_rights/tob_rights/is_tob)/学校(user_school_id) 属**全埋点空字段**，在用户行为路径上基本有字段无信息，不能用于提升路径分析准确度，已从「零成本可补」降级排除。路径场景真正可补的未接字段为 single_page_app / env_trust_click / fp_id / title / latest_inside_search_keyword / latest_traffic_source_type（latest 类需先验证填充率）。
- SOP 更新：最小输入清单补充「转化节点定义」「用户分群/预期类型」（B 类沉淀）；软化 ≤7 天硬约束为建议值，行为少可放宽（D 类）。
- C 类支付核查：用户提供支付 SQL（独立支付表，不依赖埋点 toc_rights）后，用 user_id+时间窗 与行为路径交叉验证「卡在支付」是否真实。
- 立即可做落地：抽 xlsx/csv 表头确认**实际导入数据仅 14 列**（与 _normalizeRow 现有映射一致），2.1 六字段（single_page_app/env_trust_click/fp_id/title/latest_inside_search_keyword/latest_traffic_source_type）**当前导出数据里根本没有**——光改解析层拿不到值，必须由导出 SQL 把列 SELECT 出来。已改 database.js `_normalizeRow` 新增 6 字段映射（DDL 名+中文别名双写），并在《A类字段补充需求清单.md》第五节给出可直接追加的导出 SQL 列片段 + 重导入步骤。A4 `log_event_type` 已具备（14 列中）。A1/A2/A3/A5 归「需建规则/映射后续」，A6 暂不处理。
