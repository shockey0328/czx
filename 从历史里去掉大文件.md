# 从 Git 历史里去掉大文件（避免 100MB 限制）

## 情况一：大文件只在本地，还没 push

目标：让即将 push 的提交里不包含大文件。

### 方法 A：软重置 + 排除大文件后重新提交（推荐）

在仓库根目录（`e:\橙子学数据看板\橙子学数据看板`）打开 PowerShell：

```powershell
# 1. 回到远程分支，保留所有修改在暂存区
git reset --soft origin/main

# 2. 从暂存区移除大文件（保留本地文件）
git rm --cached "用户行为看板（周度）/2026年3月8日用户行为日志.csv"
git rm --cached "用户行为看板（周度）/2026年3月8日用户行为日志.xlsx"
# 若有其他超 100MB 的文件，同样用 git rm --cached "路径" 移除

# 3. 查看状态，确认大文件已不在 “要提交” 里
git status

# 4. 一次性提交
git commit -m "feat: 看板更新，不含大 CSV/Excel"

# 5. 推送
git push
```

这样会把这之前多出来的本地提交“压成”一个不含大文件的新提交再 push。

---

## 情况二：大文件已经 push 上去了

目标：从历史里彻底删掉大文件，再强制推送。

### 方法 B：用 rebase 删掉“加入大文件”的那次提交

```powershell
# 1. 查看最近提交，找到“第一次加入大文件”的提交的上一行的 hash（记作 <上一提交>）
git log --oneline -20

# 2. 从那个提交开始做交互式 rebase
git rebase -i <上一提交>
# 例如：若 “加入大文件” 的提交是 abc1234，上一行是 def5678，则：
#   git rebase -i def5678

# 3. 在编辑器里：
#    - 找到“加入大文件”的那一行
#    - 把行首的 pick 改成 drop（或直接删掉这一行）
#    - 保存并关闭

# 4. 若 rebase 中途有冲突，按提示解决后执行：
#    git add .
#    git rebase --continue
#    直到完成

# 5. 强制推送（会改写远程历史，团队协作需先沟通）
git push --force-with-lease
```

若不想删整次提交，只想从某次提交里“拿掉一个文件”：

```powershell
git rebase -i <上一提交>
# 把对应那行的 pick 改成 edit，保存
# 然后：
git rm --cached "用户行为看板（周度）/2026年3月8日用户行为日志.csv"
git commit --amend --no-edit
git rebase --continue
git push --force-with-lease
```

### 方法 C：reset 到“大文件之前”再重新提交

```powershell
# 1. 记下“加入大文件之前”的提交 hash（例如 5edcb2f）
git log --oneline -20

# 2. 硬重置到那一次（之后的提交会消失，但改动还在工作区外）
git reset --hard <大文件之前的提交hash>

# 3. 用 cherry-pick 把“要保留”的提交一个个拣过来（跳过含大文件的）
#    git cherry-pick <提交hash>
#    若某次 pick 里带了大文件，冲突时删掉大文件再：
#    git add .
#    git cherry-pick --continue

# 4. 或：不用 cherry-pick，直接重新做一次“软重置 + 排除大文件”的流程（同情况一方法 A）
```

---

## 方法 D：Revert（不推荐用于解决 100MB）

Revert 会多一个“反向提交”，大文件仍然留在更早的历史里，**仓库体积和 100MB 限制不会变**，只适合“撤销某次提交的效果”，不适合“从历史里删大文件”。

```powershell
# 仅当你想“撤销某次提交的改动”且接受历史里仍有该文件时使用：
git revert <含大文件的提交hash> --no-commit
git rm --cached "用户行为看板（周度）/2026年3月8日用户行为日志.csv"
git commit -m "Revert: 移除大文件"
git push
```

---

## 建议

- **还没 push**：用 **情况一 · 方法 A**（软重置 + 排除大文件）即可。
- **已经 push**：用 **方法 B（rebase -i）** 或 **方法 C（reset + 重新提交）** 改写历史，再 `git push --force-with-lease`。
- 改完历史后，确保 `.gitignore` 里已忽略 `**/用户行为日志*.csv`、`**/用户行为日志*.xlsx`，避免再次误提交。
